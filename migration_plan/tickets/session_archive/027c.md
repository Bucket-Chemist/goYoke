---
id: GOgent-027c
title: "Implement AnalyzeToolDistribution()"
description: "Analyze tool usage patterns from parsed transcript to understand session characteristics"
status: pending
dependencies: [GOgent-027b]
time_estimate: "0.75h"
phase: 2
priority: CRITICAL
---

# GOgent-027c: Implement AnalyzeToolDistribution()

## Description
Analyze tool usage patterns from parsed transcript to understand session characteristics.

## Implementation Intention
Count tool usage (Read, Edit, Write, Task, Bash, etc.) to enable session phase detection and adaptive handoff formatting.

## Intended End State
- Function returns map of tool names to usage counts
- Example output: {"Read": 30, "Edit": 10, "Task": 2}
- Handles empty events slice
- Test coverage ≥80%

## Dependencies
- GOgent-027b: ParseTranscript() must be complete

## Acceptance Criteria
- [x] AnalyzeToolDistribution(events []ToolEvent) map[string]int implemented
- [x] Counts occurrences of each tool type
- [x] Handles empty events slice (returns empty map)
- [x] Handles nil events slice (returns empty map)
- [x] Handles unknown tool names gracefully
- [x] All tests pass: `go test ./pkg/routing`
- [x] Race detector clean: `go test -race ./pkg/routing`
- [x] Test coverage ≥80%
- [x] **ECOSYSTEM TEST PASS REQUIRED**: `make test-ecosystem` shows ALL PASS

## Implementation Details

### Files Modified
- `pkg/routing/transcript.go` - Add new function

### Imports
```go
package routing

// No additional imports needed beyond what ParseTranscript() requires
```

### Implementation
```go
// AnalyzeToolDistribution counts tool usage across all events
func AnalyzeToolDistribution(events []ToolEvent) map[string]int {
	distribution := make(map[string]int)

	// Handle nil slice gracefully
	if events == nil {
		return distribution
	}

	for _, event := range events {
		distribution[event.ToolName]++
	}

	return distribution
}
```

### Expected Output Format
```go
map[string]int{
	"Read":  30,
	"Edit":  10,
	"Write": 5,
	"Task":  2,
	"Bash":  8,
	"Glob":  12,
	"Grep":  15,
}
```

### Tests
```go
package routing

import (
	"testing"
)

func TestAnalyzeToolDistribution_EmptySlice(t *testing.T) {
	result := AnalyzeToolDistribution([]ToolEvent{})

	if len(result) != 0 {
		t.Errorf("Expected empty map for empty slice, got: %v", result)
	}
}

func TestAnalyzeToolDistribution_NilSlice(t *testing.T) {
	result := AnalyzeToolDistribution(nil)

	if result == nil {
		t.Error("Expected non-nil map for nil input, got nil")
	}

	if len(result) != 0 {
		t.Errorf("Expected empty map for nil slice, got: %v", result)
	}
}

func TestAnalyzeToolDistribution_SingleToolType(t *testing.T) {
	events := []ToolEvent{
		{ToolName: "Read"},
		{ToolName: "Read"},
		{ToolName: "Read"},
	}

	result := AnalyzeToolDistribution(events)

	if result["Read"] != 3 {
		t.Errorf("Expected Read count of 3, got: %d", result["Read"])
	}

	if len(result) != 1 {
		t.Errorf("Expected only 1 tool type, got: %d", len(result))
	}
}

func TestAnalyzeToolDistribution_MixedTools(t *testing.T) {
	events := []ToolEvent{
		{ToolName: "Read"},
		{ToolName: "Edit"},
		{ToolName: "Read"},
		{ToolName: "Write"},
		{ToolName: "Edit"},
		{ToolName: "Read"},
	}

	result := AnalyzeToolDistribution(events)

	expected := map[string]int{
		"Read":  3,
		"Edit":  2,
		"Write": 1,
	}

	for tool, expectedCount := range expected {
		if result[tool] != expectedCount {
			t.Errorf("Expected %s count of %d, got: %d", tool, expectedCount, result[tool])
		}
	}
}

func TestAnalyzeToolDistribution_UnknownToolNames(t *testing.T) {
	events := []ToolEvent{
		{ToolName: "CustomTool"},
		{ToolName: "AnotherTool"},
		{ToolName: "CustomTool"},
	}

	result := AnalyzeToolDistribution(events)

	if result["CustomTool"] != 2 {
		t.Errorf("Expected CustomTool count of 2, got: %d", result["CustomTool"])
	}

	if result["AnotherTool"] != 1 {
		t.Errorf("Expected AnotherTool count of 1, got: %d", result["AnotherTool"])
	}
}

func TestAnalyzeToolDistribution_AccuracyWithRealPattern(t *testing.T) {
	events := []ToolEvent{
		{ToolName: "Read"},
		{ToolName: "Read"},
		{ToolName: "Read"},
		{ToolName: "Glob"},
		{ToolName: "Glob"},
		{ToolName: "Grep"},
		{ToolName: "Grep"},
		{ToolName: "Grep"},
		{ToolName: "Edit"},
		{ToolName: "Write"},
		{ToolName: "Bash"},
		{ToolName: "Task"},
	}

	result := AnalyzeToolDistribution(events)

	expected := map[string]int{
		"Read":  3,
		"Glob":  2,
		"Grep":  3,
		"Edit":  1,
		"Write": 1,
		"Bash":  1,
		"Task":  1,
	}

	if len(result) != len(expected) {
		t.Errorf("Expected %d tool types, got: %d", len(expected), len(result))
	}

	for tool, expectedCount := range expected {
		if result[tool] != expectedCount {
			t.Errorf("Tool %s: expected count %d, got: %d", tool, expectedCount, result[tool])
		}
	}
}
```

### Test Deliverables (MANDATORY)
- [x] Test file: `pkg/routing/transcript_test.go` (functions added to existing file)
- [x] Number of test functions: 6
- [x] Coverage achieved: ≥80%
- [x] Tests passing: ✅ (output: `go test ./pkg/routing`)
- [x] Race detector clean: ✅ (output: `go test -race ./pkg/routing`)
- [x] **ECOSYSTEM TEST PASS REQUIRED**: Run `make test-ecosystem` and verify ALL PASS
- [x] Ecosystem test output saved to: `test/audit/GOgent-027c/`
- [x] Test audit updated: `/test/INDEX.md` row added

**CRITICAL**: The `make test-ecosystem` command MUST pass before ticket can be marked complete. This is NON-NEGOTIABLE.

## Time Estimate
0.75 hours

## Why This Matters
Tool distribution is the foundation for session phase detection. A session with 70% Read/Glob/Grep is "discovery", while 70% Edit/Write is "implementation". This enables adaptive handoff formatting.
