---
id: GOgent-028o
title: "ADR: JSONL Handoff Format"
description: "Architecture Decision Record documenting rationale for dual JSONL + Markdown format"
status: pending
dependencies: [GOgent-028]
time_estimate: "0.5h"
phase: 4
priority: MAJOR
---

# GOgent-028o: ADR Documentation

## Description
Create Architecture Decision Record (ADR) documenting why we chose JSONL + Markdown dual format over markdown-only or JSONL-only approaches.

## Implementation Intention
Document architectural decision for future maintainers.

## Intended End State
- ADR document in `docs/decisions/`
- Rationale, alternatives, consequences documented
- Searchable for future reference

## Dependencies
- GOgent-028: Handoff implementation must exist

## Acceptance Criteria
- [x] File created: `docs/decisions/ADR-028-jsonl-handoff-format.md`
- [x] ADR follows standard template (Status, Context, Decision, Consequences)
- [x] Documents alternatives considered (markdown-only, JSONL-only, database)
- [x] Documents rationale for dual format
- [x] Documents tradeoffs (complexity vs queryability)
- [x] Documents migration strategy from bash markdown
- [ ] Peer reviewed by another developer
- [x] **ECOSYSTEM TEST PASS REQUIRED**: `make test-ecosystem` shows ALL PASS

## Implementation Details

### ADR Document
File: `docs/decisions/ADR-028-jsonl-handoff-format.md`

```markdown
# ADR-028: JSONL Handoff Format with Markdown Rendering

**Status**: Accepted

**Date**: 2026-01-19

**SCOPE EXPANSION (2026-01-20)**: Added "Implementation Decisions" and "Testing Strategy" sections per architect analysis. Original ADR documented storage format decision but not CLI design rationale. Expanded to document implementation-level decisions for future maintainers.

**Deciders**: [Team/Individuals]

**Technical Story**: GOgent-028 series - Session handoff generation

---

## Context

The bash `session-archive.sh` hook generates session handoffs as markdown-only:
- Overwrites `.claude/memory/last-handoff.md` each session
- Human-readable format
- No machine-queryable history
- No append-only JSONL log

### Problem Statement

We need session handoffs that are:
1. **Human-readable** for immediate review
2. **Machine-queryable** for retrospective analysis
3. **Append-only** for historical tracking
4. **Schema-versioned** for future evolution
5. **Compatible** with existing load-routing-context.sh hook

### Constraints

- Must maintain compatibility with SessionStart hook (reads markdown)
- Must support querying across sessions (e.g., "violations last 10 sessions")
- Must be testable (unit tests for JSONL parsing, integration tests for workflow)
- Must handle schema evolution (versioning)

---

## Decision

**Implement dual-format handoff generation**:

1. **Primary Format: JSONL** (`handoffs.jsonl`)
   - Append-only log (never overwritten)
   - Machine-readable JSON Lines format
   - Schema versioning via `schema_version` field
   - Queryable history for retrospectives

2. **Secondary Format: Markdown** (`last-handoff.md`)
   - Rendered from JSONL on-demand
   - Human-readable for immediate review
   - Overwritten each session (latest handoff)
   - Compatible with load-routing-context.sh

### Implementation

```
SessionEnd → gogent-archive
            ↓
  GenerateHandoff(metrics)
            ↓
  Append to handoffs.jsonl (JSONL)
            ↓
  LoadHandoff(handoffs.jsonl)
            ↓
  RenderMarkdown(handoff) → last-handoff.md
            ↓
  ArchiveArtifacts()
```

### Schema

JSONL schema (v1.0):
```json
{
  "schema_version": "1.0",
  "timestamp": 1234567890,
  "session_id": "session-abc123",
  "context": {
    "metrics": {"tool_calls": 42, "errors": 5, "violations": 3},
    "git_info": {"branch": "main", "dirty": false},
    "active_ticket": "GOgent-028"
  },
  "artifacts": {
    "sharp_edges": [...],
    "violations": [...],
    "error_patterns": [...]
  },
  "actions": ["Review sharp edges", "Fix violations"]
}
```

Markdown rendered sections:
- Session Metrics
- Sharp Edges (formatted tables)
- Routing Violations (top 10)
- Recommended Actions

---

## Alternatives Considered

### Alternative 1: Markdown-Only (Status Quo)

**Pros**:
- Simple implementation
- Human-readable
- Compatible with existing hooks

**Cons**:
- ❌ No queryable history
- ❌ Can't answer "violations last 10 sessions"
- ❌ Overwrites previous handoff (data loss)
- ❌ No schema versioning
- ❌ Difficult to parse programmatically

**Rejected**: Doesn't support retrospective analysis.

### Alternative 2: JSONL-Only

**Pros**:
- Machine-queryable
- Append-only history
- Schema-versioned

**Cons**:
- ❌ Not human-readable
- ❌ Breaks load-routing-context.sh compatibility
- ❌ Requires jq or custom tooling for immediate review

**Rejected**: Breaks existing SessionStart hook, poor UX.

### Alternative 3: SQLite Database

**Pros**:
- Queryable with SQL
- Efficient indexing
- Relational data support

**Cons**:
- ❌ Adds dependency (sqlite3)
- ❌ Binary format (not human-readable)
- ❌ Requires schema migrations
- ❌ Overkill for append-only log
- ❌ Breaks load-routing-context.sh compatibility

**Rejected**: Too complex for use case.

### Alternative 4: Dual Format (Chosen)

**Pros**:
- ✅ Human-readable (markdown)
- ✅ Machine-queryable (JSONL)
- ✅ Append-only history (JSONL)
- ✅ Schema-versioned (JSONL)
- ✅ Hook-compatible (markdown)
- ✅ Testable (both formats)

**Cons**:
- ⚠️ Slightly more complex (two file writes)
- ⚠️ Potential inconsistency if markdown render fails

**Accepted**: Best balance of all requirements.

---

## Implementation Decisions (EXPANDED SCOPE)

### CLI Design

**Decision**: Single binary `gogent-archive` with STDIN-driven architecture

**Rationale**:
- **STDIN input**: SessionEnd JSON from Claude Code hook (no command-line args parsing for hook mode)
- **STDOUT output**: Confirmation JSON (hook spec compliance)
- **STDERR output**: Human-readable logs (not parsed by hook infrastructure)
- **Exit codes**: 0 = success, 1 = failure (standard Unix convention)
- **No subcommands** for hook mode (simplest possible interface)

**Alternatives considered**:
- Flag-based input (`--session-id`, `--timestamp`): ❌ Verbose, breaks hook contract
- Config file input: ❌ Adds file I/O dependency, breaks atomicity
- Env vars only: ❌ Shell escaping issues with complex JSON

**Why STDIN won**:
- ✅ Hook infrastructure streams JSON directly (no intermediate files)
- ✅ Atomic: All input arrives together
- ✅ Testable: `echo '{}' | gogent-archive`

### Project Directory Detection

**Decision**: GOGENT_PROJECT_DIR env var with fallback to `pwd`

**Rationale**:
- Hook knows project directory, sets env var
- Fallback to `pwd` enables manual invocation from project root
- No magic path guessing (e.g., searching for `.git`)

**Why not auto-detect git root?**
- ❌ Assumes project is git repo (might not be)
- ❌ Ambiguous with nested repos (submodules)
- ❌ Slower (filesystem traversal)

### Error Handling Strategy

**Decision**: Three-line error format: `[component] What. Why. How to fix.`

**Example**:
```
[gogent-archive] Failed to write handoffs.jsonl: permission denied.
  Directory .claude/memory/ is not writable.
  Fix: chmod 755 .claude/memory/ or check disk space.
```

**Rationale**:
- **What**: Clear failure description
- **Why**: Root cause diagnosis
- **How**: Actionable remediation

**Non-fatal errors**: Logged but don't fail hook (e.g., markdown render failure)
**Fatal errors**: Exit 1 with clear message (e.g., JSONL write failure)

### Testing Strategy

**Decision**: Three-tier testing pyramid

**Tier 1: Unit Tests** (`pkg/session/*_test.go`)
- ≥80% coverage requirement
- Test pure functions (RenderMarkdown, GenerateHandoff)
- Mock file I/O where possible

**Tier 2: Integration Tests** (`test/integration/*_test.go`)
- Full workflow: SessionEnd JSON → JSONL → Markdown → Archival
- Real file system (t.TempDir())
- Binary invocation via exec.Command

**Tier 3: Compatibility Tests** (`test/compatibility/*_test.sh`)
- Bash hook parity verification
- Metrics alignment (tool calls, errors, violations)
- Context loading validation (load-routing-context.sh reads markdown)

**Why this structure?**
- Unit tests catch logic bugs (fast feedback)
- Integration tests catch workflow bugs (filesystem, env vars)
- Compatibility tests catch regression bugs (metrics drift)

---

## Consequences

### Positive

1. **Queryable History**:
   - `gogent-archive list` shows all sessions
   - `gogent-archive stats` aggregates metrics
   - Custom scripts can parse JSONL

2. **Schema Evolution**:
   - `schema_version` field enables future migrations
   - Old JSONL entries readable with versioned parsers

3. **Human UX**:
   - Markdown rendering for immediate review
   - Compatible with existing workflows

4. **Testability**:
   - Unit tests for JSONL generation
   - Integration tests for markdown rendering
   - Schema validation tests

5. **Append-Only**:
   - Never lose historical data
   - Supports trend analysis over time

### Negative

1. **Implementation Complexity**:
   - Requires two file writes (JSONL append + markdown render)
   - Requires synchronization (render from JSONL, not independently)

2. **Disk Usage**:
   - Append-only JSONL grows over time
   - Mitigation: Archival rotation after N sessions (future enhancement)

3. **Potential Inconsistency**:
   - If markdown render fails, JSONL exists but markdown doesn't
   - Mitigation: RenderMarkdown is non-fatal; JSONL is source of truth

### Migration Path

From bash markdown to Go JSONL:

1. **Phase 1**: Deploy Go hook, generate both formats
2. **Phase 2**: Verify load-routing-context.sh works with Go markdown
3. **Phase 3**: Retire bash hook
4. **Phase 4**: Optionally migrate old bash handoffs to JSONL (best-effort)

### Future Enhancements

- **Querying API**: REST API for handoff queries
- **Dashboard**: Web UI for session trends
- **Rotation**: Auto-archive JSONL after 100 sessions
- **Compression**: gzip old JSONL entries

---

## Compliance

This decision aligns with:
- **XDG Base Directory Spec**: Files in `~/.claude/memory/` (user data)
- **Go Conventions**: Single binary, no runtime dependencies
- **JSONL Spec**: Newline-delimited JSON for streaming
- **Markdown Spec**: CommonMark-compatible rendering

---

## References

- Einstein GAP Document: `einstein-gap-028-integration-analysis.md`
- Implementation: `pkg/session/handoff.go`, `pkg/session/handoff_markdown.go`
- Tests: `pkg/session/handoff_test.go`, `test/integration/session_handoff_integration_test.go`
- Bash Hook (original): `~/.claude/hooks/session-archive.sh`

---

**Decision Maker**: [Name/Role]
**Approved By**: [Team/Lead]
**Review Date**: 2026-01-19
```

### Test Deliverables (MANDATORY)
- [x] ADR file created: `docs/decisions/ADR-028-jsonl-handoff-format.md`
- [x] All sections present (Status, Context, Decision, Alternatives, Consequences)
- [ ] Peer review completed
- [ ] Cross-referenced in project documentation
- [x] **ECOSYSTEM TEST PASS REQUIRED**: Run `make test-ecosystem` and verify ALL PASS
- [x] Ecosystem test output saved to: `test/audit/GOgent-028o/`
- [x] Test audit updated: `/test/INDEX.md` row added

## Why This Matters
Architecture decisions made without documentation become tribal knowledge. When original developers leave, rationale is lost. This ADR prevents future "why did we do it this way?" confusion and provides context for future changes.

**Knowledge Preservation**: Ensures decision rationale survives developer turnover.
