---
id: UX-015
title: "Conversation: collapsible tool-use blocks"
status: pending
dependencies: []
time_estimate: "0.25-0.5s"
phase: 3
priority: P2
branch: p2
rec_id: 3d
tags: [plan-generated, ux-redesign, p2, phase-3]
needs_planning: false
---

# UX-015: Conversation: collapsible tool-use blocks

## Description

ENHANCEMENT ONLY — collapse/expand infrastructure already exists at `panel.go:1006-1037` (`renderToolBlock()`). The `ToolBlock` struct has an `Expanded` boolean field (line 1019). Existing keybindings: `ToggleToolExpansion` (`alt+e` at keys.go:280), `CycleExpansion` (`alt+shift+e` at keys.go:284).

Enhancement: ensure new tool blocks default to collapsed (`Expanded=false`), verify `ToolID`-keyed state retention across re-renders, and improve the collapsed one-line summary format (status icon + tool name + truncated command).

DO NOT reimplement from scratch. Reference and extend existing code.

## Acceptance Criteria

- [ ] New tool blocks default to collapsed (`Expanded=false`)
- [ ] Collapsed one-line shows: status icon + tool name + truncated command
- [ ] Expanded view shows full command + output (existing behavior preserved)
- [ ] Collapse state keyed by `ToolID` (not array index), retained across re-renders
- [ ] Existing `ToggleToolExpansion` (`alt+e`) and `CycleExpansion` (`alt+shift+e`) still work correctly
- [ ] Restoration from provider switch resets `Expanded=false` (existing behavior at panel.go:1148-1159)

## Files

- `internal/tui/components/claude/panel.go`
- `internal/tui/model/key_handlers.go`

## Dependencies

None

## Context

Spec Rec 3d. Progressive disclosure for tool output.

## Review Notes

- Staff-Architect final review: UPGRADED to enhancement-only. Existing infrastructure at `panel.go:1006-1037` with `ToggleToolExpansion` and `CycleExpansion` bindings. Effort reduced from 0.75s to 0.25-0.5s.
- Codebase uses `ToolID` (not `tool_use_id`) — see `ToolBlock` struct and `handleToolResultMsg` at panel.go:409-415.
- Risk #5 (contractor reimplements from scratch) applies here — ticket explicitly states enhancement-only.

---

_Generated from: tickets/UX-redesign/UX-REDESIGN-SPEC.md Rec 3d_
_Braintrust review: tickets/UX-redesign/BRAINTRUST-ANALYSIS-20260411.md_
