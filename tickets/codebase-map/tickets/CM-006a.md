# CM-006a: Add TypeScript Grammar Support

**Phase**: Phase 1: Go Binary Implementation (v1 scope)
**Priority**: HIGH
**Blocked By**: CM-005c (complete binary with Go extraction working)
**Effort**: 0.5-1 day
**Parent**: CM-006 (split into CM-006a for TypeScript, CM-006b for Python + R)

## Description

Implement `LanguageExtractor` for TypeScript. TypeScript is the second language in v1 scope alongside Go, as GOgent-Fortress includes TypeScript/React code in `packages/tui/`.

Python and R support is deferred to Phase 4 (CM-006b) — uncertain benefit for the primary use case.

## Implementation Instructions

### TypeScript Extractor

**File**: `cmd/gogent-codebase-extract/typescript.go`

Implement `TypeScriptExtractor` struct satisfying `LanguageExtractor` interface:

- `Language()` → "typescript"
- `Extensions()` → [".ts", ".tsx"]
- `QuerySource()` → embedded `queries/typescript.scm`
- `IsExported(name)` → check for `export` keyword (requires node context)
- `ClassifyImport(importPath, projectRoot)`:
  - Relative paths (`./`, `../`) → internal
  - `@types/*` or known npm packages → external
  - Node.js built-in modules → stdlib

### TypeScript Queries (`queries/typescript.scm`)

As specified in CM-006:
- Function declarations
- Arrow functions assigned to const/let
- Class declarations
- Method definitions
- Interface declarations
- Type alias declarations
- Enum declarations
- Import statements
- Export detection

### TypeScript-specific Handling

- `export` keyword → `exported: true`
- Arrow functions assigned to `const` → kind="function"
- Interfaces → kind="interface"
- Type aliases → kind="type"
- Enums → kind="enum"
- `.tsx` files: same grammar, JSX nodes ignored for symbol extraction

**Behavior contracts**: Code examples describe WHAT behavior is expected. The implementing agent writes code against the library selected in CM-001.

## Success Criteria

- [ ] Extracts function declarations and arrow functions
- [ ] Extracts class and interface declarations
- [ ] Recognizes `export` keyword for exported detection
- [ ] Extracts type aliases and enums
- [ ] Import paths extracted from `import` statements
- [ ] `.tsx` files handled (JSX ignored, functions/types extracted)
- [ ] Output matches extraction.schema.json

## Test Coverage Requirements

| Test Name | Input | Expected |
|-----------|-------|----------|
| `TestTSFunction/declaration` | `function hello(): void {}` | kind=function, returns=void |
| `TestTSFunction/arrow` | `const hello = (): void => {}` | kind=function |
| `TestTSInterface` | `interface Foo { bar(): void }` | kind=interface |
| `TestTSExport` | `export function hello() {}` | exported=true |
| `TestTSClass` | `class Foo { bar() {} }` | class + method |
| `TestTSTypeAlias` | `type Foo = { x: number }` | kind=type |
| `TestTSEnum` | `enum Color { Red, Green }` | kind=enum |
| `TestTSImport` | `import { foo } from './bar'` | internal import |
| `TestTSXFile` | React component with JSX | Functions extracted, JSX ignored |

**Coverage target**: 80%+

## Files to Create/Modify

| File | Action | Purpose |
|------|--------|---------|
| `cmd/gogent-codebase-extract/typescript.go` | Create | TypeScript extractor |
| `cmd/gogent-codebase-extract/queries/typescript.scm` | Create | TypeScript queries |
| `cmd/gogent-codebase-extract/typescript_test.go` | Create | TypeScript tests |
| `cmd/gogent-codebase-extract/testdata/typescript/` | Create | TypeScript test fixtures |
| `go.mod` | Modify | Add TypeScript grammar dependency |

## Dependencies and Integration Points

**Consumes from:**
- CM-005c: Complete binary with `LanguageExtractor` interface and Go extraction

**Produces for:**
- CM-007: TypeScript call query support
- CM-008: TypeScript test fixtures
- CM-009: Multi-language extraction for fork skill

## Acceptance Checklist

- [ ] TypeScript symbols extracted from test fixtures
- [ ] Output matches extraction.schema.json
- [ ] `go build` succeeds with TypeScript grammar linked
- [ ] Import classification works (relative=internal, npm=external)
- [ ] Language auto-detection from `.ts`/`.tsx` extension works
- [ ] Tested on `packages/tui/src/` files from this repo

**Source**: CM-006 split (review finding CB2)
