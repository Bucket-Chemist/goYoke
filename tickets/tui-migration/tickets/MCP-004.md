---
id: MCP-004
title: "Integration test, settings.json docs, tickets-index append"
status: completed
dependencies: [MCP-003]
time_estimate: "3-4 hours"
phase: 11
tags: [mcp-standalone, phase-11, plan-generated]
agent: "go-pro"
needs_planning: false
coverage_target: 80
---

# MCP-004: Integration test, settings.json docs, tickets-index append

## Description

Complete the MCP standalone binary work with an integration test suite, setup documentation, and tickets-index update.

**Key deliverables:**
- `cmd/gofortress-mcp-standalone/integration_test.go` — in-process MCP server integration tests
- `docs/mcp-standalone-setup.md` — installation and `settings.json` configuration guide
- `tickets/tui-migration/tickets/MCP-001.md` through `MCP-004.md` — ticket documentation
- `tickets/tui-migration/tickets/tickets-index.json` — MCP tickets marked completed

**Integration test coverage:**
- Tool manifest (both tools listed)
- Ping with and without echo
- Spawn with unknown agent (soft error response)
- Uses `mcpsdk.NewInMemoryTransports()` — no real I/O required

**Documentation covers:**
- `go build` installation
- `settings.json` `mcpServers` block with `GOGENT_PROJECT_ROOT`
- All environment variables (`GOGENT_NESTING_LEVEL`, `GOGENT_PARENT_AGENT`, etc.)
- Coexistence table with `gofortress-mcp`

## Acceptance Criteria

- [x] `integration_test.go` with `//go:build integration` tag
- [x] `TestIntegration_ToolManifest` — both tools listed
- [x] `TestIntegration_Ping` — PONG response with timestamp
- [x] `TestIntegration_SpawnAgent_UnknownAgent` — soft error in output
- [x] `docs/mcp-standalone-setup.md` covers installation + settings.json config
- [x] All MCP ticket .md files created with correct frontmatter
- [x] `tickets-index.json` updated: all MCP-001–004 marked `completed`
- [x] `go test -tags=integration ./cmd/gofortress-mcp-standalone/...` passes

## Files

### Target Packages
- `cmd/gofortress-mcp-standalone/integration_test.go` — new file
- `docs/mcp-standalone-setup.md` — new file
- `tickets/tui-migration/tickets/MCP-001.md` through `MCP-004.md` — new files
- `tickets/tui-migration/tickets/tickets-index.json` — updated

## Context

**Phase:** 11 — Standalone MCP Binary
**Agent:** go-pro
**Coverage Target:** 80%

**Design:** Integration tests use in-process transports to avoid requiring a pre-built binary or real subprocess invocation. The `//go:build integration` tag keeps them opt-in so normal `go test ./...` stays fast.
