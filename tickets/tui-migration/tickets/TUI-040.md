---
id: TUI-040
title: "Performance benchmarks"
status: complete
dependencies: [TUI-016, TUI-017, TUI-020]
time_estimate: "3-4 hours"
phase: 9
tags: [testing, phase-9, plan-generated]
agent: "go-pro"
needs_planning: false
coverage_target: 0
completed_at: "2026-03-23"
---

# TUI-040: Performance benchmarks

## Description

Create performance benchmarks in internal/tui/bench_test.go. (1) BenchmarkStartup: measure time from program creation to ready state (SystemInitMsg processed). Target: under 200ms (Go binary startup, no Node.js). (2) BenchmarkModalRoundTrip: measure time from ModalRequestMsg to ModalResponseMsg. Target: under 100ms (in-process channel, should be <1ms). (3) BenchmarkNDJSONParsing: parse 1000 NDJSON lines from a captured session. Measure throughput. Target: sustain 10,000 lines/sec (streaming events arrive at ~100/sec). (4) BenchmarkViewRendering: render View() at 120x40 with 100 messages, 10 agents, 3 teams. Target: under 16ms (60fps). (5) BenchmarkUDSRoundTrip: measure IPC round-trip between TUI bridge and MCP server. Target: under 5ms. Use testing.B benchmarks. Create test fixtures for realistic data volumes. Report results in benchmark output format compatible with benchstat.

## Acceptance Criteria

- [x] Startup benchmark: measured and reported
- [x] Modal round-trip benchmark: under 100ms
- [x] NDJSON parsing throughput: measured and reported
- [x] View rendering benchmark: measured at 120x40
- [x] UDS round-trip benchmark: under 5ms
- [x] Results in benchstat-compatible format
- [x] Realistic test fixtures for data volumes

## Files

### Benchmark Files Added (4 packages)
- `internal/tui/cli/bench_test.go` — NDJSON parsing benchmarks (177 lines)
- `internal/tui/components/modals/bench_test.go` — Modal round-trip benchmarks (140 lines)
- `internal/tui/mcp/bench_test.go` — UDS round-trip benchmarks (203 lines)
- `internal/tui/model/bench_test.go` — Startup + view rendering benchmarks (199 lines)

### Related Files
- `.claude/conventions/go.md` — Testing patterns

## Context

**Phase:** 9 — Testing Phase
**Agent:** go-pro
**Coverage Target:** 0% (benchmarks, not coverage targets)
**Tests Required:** true

**Use "Go 1.25+" as the minimum Go version constraint (not "Go 1.22+") per review C-1.**

Depends on TUI-016 (startup path), TUI-017 (modal system), TUI-020 (agent tree). Results in benchstat-compatible format allow tracking performance regressions across commits.

## Benchmark Results (2026-03-23)

Hardware: AMD Ryzen AI 7 350 w/ Radeon 860M, Linux amd64
Run command: `go test -bench=. -benchmem -count=3` per package

### BenchmarkStartup (target: < 200 ms)

```
BenchmarkStartup-16    3860    318863 ns/op    205317 B/op    1286 allocs/op
BenchmarkStartup-16    4095    303817 ns/op    212167 B/op    1286 allocs/op
BenchmarkStartup-16    3506    318377 ns/op    198232 B/op    1286 allocs/op
```

**Result: ~307–319 µs (0.31 ms) — PASSES target (< 200 ms) by 640x margin**

### BenchmarkModalRoundTrip (target: < 100 ms)

```
BenchmarkModalRoundTrip-16    592270    2031 ns/op    11672 B/op    5 allocs/op
BenchmarkModalRoundTrip-16    577342    2274 ns/op    11672 B/op    5 allocs/op
BenchmarkModalRoundTrip-16    565057    2076 ns/op    11672 B/op    5 allocs/op
```

**Result: ~2.0–2.3 µs — PASSES target (< 100 ms) by 48,000x margin**

Additional modal benchmarks:
- `BenchmarkModalRoundTripWithChannel`: ~1.8–2.0 µs (channel send path, production code path)
- `BenchmarkModalQueueBurst` (10 modals): ~16.0–16.3 µs total (~1.6 µs/modal)

### BenchmarkNDJSONParsing (target: >= 10,000 lines/sec)

```
BenchmarkNDJSONParsing-16    209942    5071 ns/op    1442 B/op    25 allocs/op
BenchmarkNDJSONParsing-16    211534    5266 ns/op    1442 B/op    25 allocs/op
BenchmarkNDJSONParsing-16    220756    5233 ns/op    1442 B/op    25 allocs/op
```

**Result: ~5071–5266 ns/op → ~190,000–197,000 lines/sec — PASSES target (10,000 lines/sec) by 19x margin**

Per-type breakdown:
- `SystemHook`: 3.0–3.1 µs (fastest typed path)
- `StreamEvent`: 3.8–4.2 µs
- `SystemInit`: 4.7–4.9 µs
- `User/ToolResult`: 4.7–5.4 µs
- `Result/Success`: 5.4–5.7 µs
- `Assistant/Text`: 6.0–6.3 µs
- `Assistant/ToolUse`: 7.7–9.5 µs
- `Assistant/Task`: 8.5–9.4 µs
- `Unknown` (fallback): 0.9–1.0 µs (fast path, no full unmarshal)
- `BlankLine` (fast skip): 6.6–7.1 ns (zero-alloc fast path)

`BenchmarkNDJSONParsing1000Lines`: 0.19 MB/s throughput column (SetBytes(1000) = lines/s ÷ 1000)

### BenchmarkViewRendering (target: < 16 ms)

```
BenchmarkViewRendering-16    1434    815909 ns/op    912543 B/op    1817 allocs/op
BenchmarkViewRendering-16    1334    839641 ns/op    894106 B/op    1817 allocs/op
BenchmarkViewRendering-16    1561    775537 ns/op    865381 B/op    1817 allocs/op
```

**Result: ~776–840 µs (0.78–0.84 ms) — PASSES target (< 16 ms) by 19x margin**

`BenchmarkViewRenderingWithAgents` (10 agent tree nodes): ~845–891 µs — negligible overhead from agent tree population.

### BenchmarkUDSRoundTrip (target: < 5 ms)

```
BenchmarkUDSRoundTrip-16    150330    9423 ns/op    1077 B/op    29 allocs/op
BenchmarkUDSRoundTrip-16    114870    9529 ns/op    1077 B/op    29 allocs/op
BenchmarkUDSRoundTrip-16    121434    8249 ns/op    1077 B/op    29 allocs/op
```

**Result: ~8.2–9.5 µs (0.0082–0.0095 ms) — PASSES target (< 5 ms) by 500x margin**

`BenchmarkUDSRoundTripParallel`: ~9.2–10.2 µs under parallel load (mutex serialization overhead visible).

### Summary: All Targets Met

| Benchmark | Target | Measured | Margin |
|-----------|--------|----------|--------|
| BenchmarkStartup | < 200 ms | ~0.31 ms | 645x under |
| BenchmarkModalRoundTrip | < 100 ms | ~0.002 ms | 48,000x under |
| BenchmarkNDJSONParsing | >= 10,000 lines/sec | ~195,000 lines/sec | 19x over target |
| BenchmarkViewRendering | < 16 ms | ~0.82 ms | 20x under |
| BenchmarkUDSRoundTrip | < 5 ms | ~0.009 ms | 550x under |

All 5 target benchmarks pass with large safety margins. The bottleneck for real-world performance will be the Claude CLI subprocess latency (~100–2000ms per response), not Go in-process operations.

---

_Generated from: .claude/sessions/20260316-plan-tickets-tui/implementation-plan.json_
_Review: .claude/sessions/20260316-plan-tickets-tui/review-critique.md_
