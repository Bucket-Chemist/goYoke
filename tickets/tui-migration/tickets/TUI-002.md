---
id: TUI-002
title: "Spike - Go MCP SDK POC with Claude CLI"
status: pending
dependencies: []
time_estimate: "1-2 hours"
phase: 1
tags: [spike, phase-1, plan-generated]
agent: "go-pro"
needs_planning: false
coverage_target: 0
---

# TUI-002: Spike - Go MCP SDK POC with Claude CLI

## Description

Build a minimal Go MCP server (~60 lines) using github.com/modelcontextprotocol/go-sdk v1.2.0 with mcp.StdioTransport{} and verify Claude Code CLI can discover and invoke it. Steps: (1) Create cmd/gofortress-mcp-poc/main.go that registers one tool `test_mcp_ping` using the Go MCP SDK. Tool takes no arguments and returns {status: 'pong', timestamp: <unix>}. (2) Build the binary. (3) Create a test .mcp.json config: {"mcpServers": {"gofortress-poc": {"type": "stdio", "command": "/absolute/path/to/gofortress-mcp-poc"}}}. (4) Run Claude CLI with this MCP config and ask it to call test_mcp_ping. (5) Verify the tool is discovered in the system.init event's tools[] array. (6) Verify the tool is invocable and returns correct results. (7) Document findings in tickets/tui-migration/spike-results/go-mcp-poc.md. Include: SDK API patterns used, any gotchas, tool schema registration pattern, and confirmation of stdio transport functionality.

## Review Amendments

**⚠️ Review C-2:** Go MCP SDK version mismatch: plan says "Go MCP SDK v1.2.0" in constraints but strategy references "v1.3.0" throughout; go.mod has v1.2.0. The spike MUST resolve this: does v1.2.0 have `mcp.StdioTransport{}`? If not, the upgrade is a prerequisite. Use whichever version is actually deployed. Document findings in the spike output.

## Acceptance Criteria

- [ ] Go MCP server binary compiles and runs
- [ ] Claude CLI discovers the test_mcp_ping tool via --mcp-config stdio
- [ ] Tool invocation returns correct pong response
- [ ] SDK patterns documented (server creation, tool registration, handler signatures)
- [ ] Spike document written to tickets/tui-migration/spike-results/go-mcp-poc.md

## Files

### Target Packages
- `cmd/gofortress-mcp-poc` — minimal POC MCP server binary

### Related Files
- `go.mod` — Already has go-sdk v1.2.0 dependency
- `tickets/tui-migration/braintrust-handoff-v2.md` — Section 4.2 describes the Go MCP SDK validation

## Context

**Phase:** 1 — Spike Phase
**Agent:** go-pro
**Coverage Target:** 0%
**Tests Required:** false

**Use "Go 1.25+" as the minimum Go version constraint (not "Go 1.22+") per review C-1.**
**Use "Go MCP SDK v1.2.0 (verify v1.3.0 upgrade need per review C-2)" when referencing SDK version.**

This spike validates the core two-process topology assumption. If mcp.StdioTransport{} does not exist in v1.2.0, run `go get github.com/modelcontextprotocol/go-sdk@v1.3.0` and verify it exists, then update go.mod. Spike failure here requires replanning the entire architecture.

---

_Generated from: .claude/sessions/20260316-plan-tickets-tui/implementation-plan.json_
_Review: .claude/sessions/20260316-plan-tickets-tui/review-critique.md_
