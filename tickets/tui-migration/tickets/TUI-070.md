---
id: TUI-070
title: "Verify-parity refresh and integration test"
status: pending
dependencies: [TUI-043, TUI-044, TUI-045, TUI-046, TUI-047, TUI-048, TUI-049, TUI-050, TUI-051, TUI-052, TUI-053, TUI-054, TUI-055, TUI-056, TUI-057, TUI-058, TUI-059, TUI-060, TUI-061, TUI-062, TUI-063, TUI-064, TUI-065, TUI-066, TUI-067, TUI-068, TUI-069]
time_estimate: "4-6 hours"
phase: 10
tags: [ux-overhaul, phase-10g, plan-generated]
agent: "go-pro"
needs_planning: false
coverage_target: 80
---

# TUI-070: Verify-parity refresh and integration test

## Description

Final gate for Phase 10: create a comprehensive integration test suite that verifies all 28 Phase 10 features are implemented and functional. Update `ARCHITECTURE.md` with Phase 10 structural changes. Update `tickets-index.json` with the final ticket set.

**Integration test file:** `internal/tui/phase10_integration_test.go`

Create at minimum 10 integration test functions (2 per feature category, one per major feature group):
1. `TestPhase10a_AppDecomposition` — verifies `app.go` < 450 lines, handler files exist
2. `TestPhase10b_SemanticColors` — verifies `ThemeVariant`, semantic methods on `Theme`
3. `TestPhase10b_IconSystem` — verifies `IconSet`, `Theme.Icons()`, ASCII/Unicode modes
4. `TestPhase10c_StatusLineEnhancements` — verifies context bar, semantic colors, new fields
5. `TestPhase10c_SettingsTree` — verifies navigation, `SettingChangedMsg` emission
6. `TestPhase10d_ShiftTabNavigation` — verifies `shift+tab` → `FocusPrev()`, `CycleProvider` on new binding
7. `TestPhase10d_SlashCommandDropdown` — verifies dropdown activation, filtering, command list
8. `TestPhase10e_ResponsiveLayout` — verifies 4-tier breakpoints at boundary widths
9. `TestPhase10e_SearchOverlay` — verifies `SearchSource` interface, result merging
10. `TestPhase10f_SpringAnimation` — verifies harmonica integration, `AnimateTickMsg`

**ARCHITECTURE.md updates:**
- Section 16 header: update TUI v2 migration status to include Phase 10 (TUI-043 through TUI-070)
- Section 16.3 Phase Summary table: add Phase 10 row with status COMPLETE
- Version history: add v1.8 entry documenting Phase 10 completion

**tickets-index.json:** Already updated as part of this planning workflow (see the tickets-index.json file). Verify the file contains all TUI-043 through TUI-070 entries.

**Final validation:** `go test ./internal/tui/...` passes with 0 failures. Expected total: ~1,524 tests (existing 1,074 + ~450 new). `go build ./...` clean.

## Review Amendments

**m-6 (Minor — should address):** The review recommended at least 2 integration tests per category (10 minimum), not the originally planned 7. This ticket implements at least 10 integration test functions.

## Acceptance Criteria

- [ ] `internal/tui/phase10_integration_test.go` created with 10+ test functions
- [ ] All Phase 10 feature categories covered (10a through 10g)
- [ ] `go test ./internal/tui/...` passes with 0 failures
- [ ] `go build ./...` succeeds
- [ ] `ARCHITECTURE.md` Section 16 updated with Phase 10 row
- [ ] `ARCHITECTURE.md` version history entry v1.8 added
- [ ] `tickets-index.json` contains all TUI-043 through TUI-070 entries
- [ ] Total test count: original 1,074 + ~450 new Phase 10 tests
- [ ] Coverage report shows 80%+ on every new or modified file
- [ ] 80% test coverage for new/modified code

## Files

### Target Packages
- `internal/tui/` — create `phase10_integration_test.go`
- `docs/` — update `ARCHITECTURE.md`
- `tickets/tui-migration/tickets/` — verify `tickets-index.json` completeness

### Related Files
- `internal/tui/phase10_integration_test.go` — new integration test file
- `docs/ARCHITECTURE.md` — update Section 16
- `tickets/tui-migration/tickets/tickets-index.json` — verify completeness

## Context

**Phase:** 10g — Integration and Documentation
**Agent:** go-pro
**Coverage Target:** 80%

**Design:** Integration tests verify behavioral correctness across Phase 10 features. They are broader than unit tests but faster than E2E tests. This follows the pattern established by `TUI-036` (component unit tests) and `TUI-039` (E2E smoke test) in Phase 9.

**Post-Phase 10 gate:** This ticket is the final gate. If any acceptance criterion fails, the failing item blocks Phase 10 completion.

---

_Generated from: .claude/sessions/20260323-plan-tickets-tui-phase10/specs.md_
_Review: .claude/sessions/20260323-plan-tickets-tui-phase10/review-critique.md_
