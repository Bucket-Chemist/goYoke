---
id: TUI-039
title: "E2E smoke test with live CLI"
status: completed
dependencies: [TUI-016, TUI-018]
time_estimate: "4-6 hours"
phase: 9
tags: [testing, phase-9, plan-generated]
agent: "go-pro"
needs_planning: false
coverage_target: 0
---

# TUI-039: E2E smoke test with live CLI

## Description

Create an end-to-end smoke test in internal/tui/e2e_test.go (build-tagged with //go:build e2e). This test requires a real Claude CLI installation. Test flow: (1) Build gofortress and gofortress-mcp binaries. (2) Start gofortress binary in a test harness (not in a real terminal -- use tea.NewProgram with tea.WithInput(reader) and tea.WithOutput(writer)). (3) Wait for ready state (SystemInitMsg received). (4) Send a simple message ('say hello'). (5) Wait for assistant response (AssistantMsg received). (6) Wait for result (ResultMsg received). (7) Verify cost > 0, tokens > 0. (8) Send shutdown. (9) Verify clean exit (no orphan processes, session saved). Also test: (a) MCP tool discovery (testMcpPing in tools list). (b) Permission prompt (trigger a Write, handle the permission modal). (c) Session resume (start with --session-id from previous run). This test uses the real Claude API and costs money -- guard with build tag and document the cost expectation (~$0.05 per run).

## Acceptance Criteria

- [x] E2E test sends message and receives response from real CLI
- [x] Cost and token data verified in result
- [x] MCP tool discovery verified
- [x] Clean shutdown verified (no orphan processes)
- [x] Test guarded with //go:build e2e tag
- [x] Cost expectation documented
- [x] Session resume works across test runs

## Files

### Target Packages
- `internal/tui` — create e2e_test.go (build-tagged)

### Related Files
- `cmd/gofortress/main.go` — Binary under test
- `cmd/gofortress-mcp/main.go` — MCP server binary

## Context

**Phase:** 9 — Testing Phase
**Agent:** go-pro
**Coverage Target:** 0% (E2E, not unit coverage)
**Tests Required:** true

**Use "Go 1.25+" as the minimum Go version constraint (not "Go 1.22+") per review C-1.**

Depends on TUI-016 (full startup wiring) and TUI-018 (permission flow). This is the critical path endpoint: task-001 -> task-012 -> task-013 -> task-017 -> task-018 -> task-039. Build tag `//go:build e2e` guards against accidental CI execution. Estimated cost: ~$0.05 per run.

---

_Generated from: .claude/sessions/20260316-plan-tickets-tui/implementation-plan.json_
_Review: .claude/sessions/20260316-plan-tickets-tui/review-critique.md_
