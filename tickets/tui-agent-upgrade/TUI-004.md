---
id: TUI-004
title: "Extend team polling to capture stream file sizes"
status: pending
dependencies: [TUI-001]
time_estimate: "45min"
phase: 4
tags: [plan-generated, phase-4, data-pipeline]
needs_planning: false
---

# TUI-004: Extend team polling to capture stream file sizes

## Description

Extend `TeamState` with stream file size tracking and add `MostRecentRunning()` method to `TeamRegistry`. Modify the existing team directory scanning to stat stream files during the 2s poll cycle.

## Acceptance Criteria

- [ ] `TeamState` has `StreamSizes map[string]int64` field with deep copy in `copyOf()`
- [ ] `Update()` accepts `streamSizes` parameter
- [ ] `MostRecentRunning()` returns the running team, or most recent team as fallback
- [ ] `scanTeamsDir()` stats `stream_{agent}.ndjson` files and passes sizes to `Update()`
- [ ] Existing tests updated for new `Update()` signature (pass nil for streamSizes)
- [ ] `go build ./...` and `go test ./...` pass

## Files

- `internal/tui/components/teams/state.go` — `StreamSizes` field, `copyOf()`, `Update()` signature, `MostRecentRunning()` (~40 lines)
- `internal/tui/components/teams/list.go` — stat stream files in `scanTeamsDir()` (~15 lines)
- `internal/tui/components/teams/state_test.go` — Updated tests for new `Update()` signature (~5 lines)
- `internal/tui/components/teams/list_test.go` — Updated tests (~5 lines)

## Context

Stream files follow the pattern `stream_{agent}.ndjson` in the team directory. Use `os.Stat()` (metadata-only syscall, negligible cost) to get file sizes. The existing 2s polling goroutine already scans team directories — extend it rather than adding a new goroutine.

`MostRecentRunning()` logic: iterate teams, return first with status "running". If none running, return the team with the most recent `started_at` timestamp.

**Call sites for `Update()` that need signature update:**
- `scanTeamsDir()` at list.go:314
- 8 calls in state_test.go (lines 48, 64, 83, 107, 212, 236)
- 10+ calls in list_test.go (lines 121, 183, 211, 226-228, 254, 305, 350, 433, 470)
- Pass `nil` for streamSizes at all existing call sites.

---

_Generated from: SESSION_DIR/specs.md Phase 4_
