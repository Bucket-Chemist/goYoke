---
id: TUI-003
title: "Add teamsHealthWidget interface and sharedState wiring"
status: pending
dependencies: []
time_estimate: "20min"
phase: 3
tags: [plan-generated, phase-3, interface]
needs_planning: false
---

# TUI-003: Add teamsHealthWidget interface and sharedState wiring

## Description

Add the `teamsHealthWidget` interface to decouple the AppModel from the concrete health dashboard implementation. Wire the interface into `sharedState` and add the `showHealthInAgents` toggle to `AppModel`.

## Acceptance Criteria

- [ ] `teamsHealthWidget` interface defined in `interfaces.go` with `View() string`, `SetSize(w, h int)`, `SetTier(LayoutTier)`
- [ ] `sharedState` has `teamsHealth teamsHealthWidget` field
- [ ] `AppModel` has `showHealthInAgents bool` field
- [ ] `SetTeamsHealth` setter exists on `AppModel`
- [ ] `go build ./...` passes

## Files

- `internal/tui/model/interfaces.go` — New `teamsHealthWidget` interface (~10 lines)
- `internal/tui/model/app.go` — `sharedState` field + `AppModel` field + setter (~15 lines)

## Context

```go
// interfaces.go
type teamsHealthWidget interface {
    View() string
    SetSize(w, h int)
    SetTier(tier LayoutTier)
}
```

This follows the existing pattern used by `dashboardWidget`, `settingsWidget`, etc. in `interfaces.go`. The interface prevents the `teams` component package from importing `model` (maintaining acyclic imports).

---

_Generated from: SESSION_DIR/specs.md Phase 3_
