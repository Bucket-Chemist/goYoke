---
id: TUI-008
title: "Wire health dashboard in main.go and extend layout tests"
status: pending
dependencies: [TUI-002, TUI-005, TUI-006]
time_estimate: "45min"
phase: 6
tags: [plan-generated, phase-6, wiring, tests]
needs_planning: false
---

# TUI-008: Wire health dashboard in main.go and extend layout tests

## Description

Wire the `TeamsHealthModel` into the application's initialization code and extend the existing `layout_test.go` with additional regression tests for the new layout computations.

## Acceptance Criteria

- [ ] `TeamsHealthModel` created in `main.go` after `TeamListModel` and wired via `SetTeamsHealth()`
- [ ] `computeDrawerLayout` covered by table-driven tests (if not already comprehensive from TUI-002)
- [ ] `computeLayout` regression tests updated for new drawer-aware height computations
- [ ] `go build ./...` and `go test ./...` pass
- [ ] All 8 existing `layout_test.go` tests continue to pass

## Files

- Application `main.go` or `cmd/gogent-tui/main.go` — Wire `TeamsHealthModel` (~3 lines)
- `internal/tui/model/layout_test.go` — **Extend existing** file (254 lines, 8 tests) with new test cases (~40 lines added)

## Context

### Wiring pattern:
```go
// After team list creation:
teamsHealth := teams.NewTeamsHealthModel(teamRegistry)
appModel.SetTeamsHealth(&teamsHealth)
```

Follow the existing pattern used for `dashboard`, `settings`, `telemetry`, `planPreview` wiring in the initialization code.

### Test cases to add:
- `TestComputeDrawerLayout_AllTiers` — Table-driven: Compact returns (0,0); Standard/Wide/Ultra return correct heights for content/minimized states
- `TestComputeDrawerLayout_HeightBounds` — Edge cases: very small terminals, max cap enforcement, min floor enforcement
- `TestComputeLayout_WithDrawerSubtraction` — Verify panel heights = contentHeight - drawerHeight

## Review Notes

- **M-3 (review condition):** This file **extends** the existing `layout_test.go` — do NOT create a new file or overwrite existing tests. The file has 254 lines with 8 test functions including `TestComputeLayout_TierBoundaries` (12 table-driven cases).
- Some `computeDrawerLayout` tests may already exist from TUI-002 — extend coverage, don't duplicate.

---

_Generated from: SESSION_DIR/specs.md Phase 4_
