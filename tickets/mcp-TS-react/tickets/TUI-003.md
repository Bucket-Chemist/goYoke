---
id: TUI-003
title: Project Setup & Build Configuration
description: Create the packages/tui/ directory structure with all configuration files for TypeScript, esbuild, and development tooling.
status: completed
time_estimate: 45m
dependencies: [TUI-001, TUI-002]
phase: 1
tags: [plan-generated, phase-1, scaffolding]
needs_planning: false
agent: scaffolder
completed_date: 2026-02-03
---

# TUI-003: Project Setup & Build Configuration

## Description

Create the `packages/tui/` directory structure with all configuration files for TypeScript, esbuild, and development tooling.

## Acceptance Criteria

- [x] `packages/tui/` directory created
- [x] `package.json` with all dependencies (271 packages)
- [x] `tsconfig.json` with strict mode enabled
- [x] esbuild configured for production builds
- [x] tsx configured for development
- [x] **`src/config/theme.ts` created with centralized color/style constants**
- [x] `npm install` completes without errors
- [x] `npm run build` produces output (1.4MB dist/index.js)
- [x] `npm run dev` starts watch mode

## Tasks

1. Create directory structure:
   ```
   packages/
   └── tui/
       ├── package.json
       ├── tsconfig.json
       ├── src/
       │   ├── index.tsx
       │   └── config/
       │       └── theme.ts
       ├── bin/
       │   └── gofortress-tui.js
       └── tests/
   ```

2. Initialize `package.json` (from plan Section 7.1):
   ```json
   {
     "name": "@gofortress/tui",
     "version": "1.0.0",
     "type": "module",
     "main": "dist/index.js",
     "bin": {
       "gofortress-tui": "./bin/gofortress-tui.js"
     },
     "scripts": {
       "dev": "tsx watch src/index.tsx",
       "build": "esbuild src/index.tsx --bundle --platform=node --target=node22 --outfile=dist/index.js",
       "start": "node dist/index.js",
       "typecheck": "tsc --noEmit",
       "test": "vitest",
       "lint": "eslint src/"
     },
     "dependencies": {
       "@anthropic-ai/claude-agent-sdk": "^1.0.0",
       "ink": "^5.0.0",
       "ink-text-input": "^6.0.0",
       "ink-select-input": "^6.0.0",
       "ink-spinner": "^5.0.0",
       "react": "^18.3.0",
       "zustand": "^5.0.0",
       "zod": "^3.23.0",
       "chokidar": "^4.0.0",
       "commander": "^12.0.0",
       "marked": "^15.0.0",
       "marked-terminal": "^7.0.0"
     },
     "devDependencies": {
       "@types/node": "^22.0.0",
       "@types/react": "^18.3.0",
       "esbuild": "^0.24.0",
       "tsx": "^4.19.0",
       "typescript": "^5.6.0",
       "vitest": "^2.0.0",
       "eslint": "^9.0.0",
       "@typescript-eslint/eslint-plugin": "^8.0.0",
       "ink-testing-library": "^4.0.0"
     }
   }
   ```

3. Configure `tsconfig.json` (from plan Section 7.2):
   ```json
   {
     "compilerOptions": {
       "target": "ES2022",
       "module": "NodeNext",
       "moduleResolution": "NodeNext",
       "lib": ["ES2022"],
       "strict": true,
       "esModuleInterop": true,
       "skipLibCheck": true,
       "forceConsistentCasingInFileNames": true,
       "resolveJsonModule": true,
       "declaration": true,
       "declarationMap": true,
       "sourceMap": true,
       "outDir": "./dist",
       "rootDir": "./src",
       "jsx": "react-jsx",
       "jsxImportSource": "react",
       "noUncheckedIndexedAccess": true,
       "noImplicitOverride": true,
       "noPropertyAccessFromIndexSignature": true
     },
     "include": ["src/**/*"],
     "exclude": ["node_modules", "dist"]
   }
   ```

4. Create bin entry point:
   ```javascript
   #!/usr/bin/env node
   import "../dist/index.js";
   ```

5. Create centralized theme (`src/config/theme.ts`):
   ```typescript
   /**
    * Centralized theme constants for GOfortress TUI.
    * All color/style values should be imported from here.
    * See: Gemini audit GAP-1 (theming)
    */

   export const colors = {
     // Primary palette
     primary: "cyan",
     secondary: "blue",
     accent: "magenta",

     // Semantic colors
     success: "green",
     warning: "yellow",
     error: "red",
     muted: "gray",

     // Panel states
     focused: "cyan",
     unfocused: "gray",

     // Agent status colors
     agentSpawning: "yellow",
     agentRunning: "blue",
     agentComplete: "green",
     agentError: "red",

     // Message roles
     userMessage: "cyan",
     assistantMessage: "white",
     systemMessage: "gray",
   } as const;

   export const borders = {
     panel: "single",
     modal: "double",
     banner: "round",
     input: "single",
   } as const;

   export const icons = {
     agentSpawning: "◐",
     agentRunning: "●",
     agentComplete: "✓",
     agentError: "✗",
     treeIndent: "│",
     treeBranch: "├─",
     treeLeaf: "└─",
   } as const;

   export type ThemeColor = typeof colors[keyof typeof colors];
   export type BorderStyle = typeof borders[keyof typeof borders];
   ```

6. Run `npm install` and verify

## Files

- `packages/tui/package.json` — Package manifest
- `packages/tui/tsconfig.json` — TypeScript config
- `packages/tui/bin/gofortress-tui.js` — CLI entry point
- `packages/tui/src/index.tsx` — Placeholder entry
- `packages/tui/src/config/theme.ts` — **Centralized theme constants (GAP-1)**

## Context

From plan Section 5 (Phase 1):
> Create `packages/tui/` directory, Initialize `package.json` with dependencies, Configure `tsconfig.json` (strict mode)

---

*Generated from: TUI-TS-INK-CONVERSION-PLAN.md Phase 1*

## Completion Summary

**Status:** ✅ COMPLETE

### Files Created

1. **Directory Structure:**
   - `packages/tui/bin/` - CLI entry point
   - `packages/tui/src/` - Source code
   - `packages/tui/src/config/` - Configuration
   - `packages/tui/tests/` - Tests

2. **Configuration Files:**
   - `package.json` - 14 dependencies, 8 devDependencies, 6 scripts
   - `tsconfig.json` - Strict mode, ES2022, NodeNext, JSX support
   - `src/config/theme.ts` - Centralized theme (GAP-1 addressed)
   - `bin/gofortress-tui.js` - CLI entry point
   - `src/index.tsx` - Placeholder app component

### Verification Results

| Check | Status | Details |
|-------|--------|---------|
| npm install | ✅ PASS | 271 packages installed |
| npm run build | ✅ PASS | 1.4MB dist/index.js produced |
| npm run dev | ✅ PASS | tsx watch mode active |
| npm run typecheck | ✅ PASS | TypeScript checks passing |

### Technical Details

- **Build System:** esbuild with ESM format, external React DevTools
- **TypeScript:** Strict mode enabled, all strict flags active
- **Theme System:** Centralized colors, borders, icons (GAP-1)
- **Dependencies:** All specified packages installed correctly

**Ready for TUI-004:** App shell and Ink layout spike
