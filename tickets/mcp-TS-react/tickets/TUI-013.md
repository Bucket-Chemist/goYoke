---
id: TUI-013
title: MCP Integration Testing
status: pending
dependencies: [TUI-012, TUI-009]
phase: 4
tags: [plan-generated, phase-4, testing, integration]
needs_planning: false
agent: typescript-pro
---

# TUI-013: MCP Integration Testing

## Description

End-to-end testing of MCP tool integration: Claude calls tools, modals appear, user responds, tool returns result.

## Acceptance Criteria

- [ ] ask_user tool → modal → response flow works
- [ ] confirm_action tool flow works
- [ ] request_input tool flow works
- [ ] select_option tool flow works
- [ ] Concurrent tool calls handled correctly
- [ ] Error cases handled gracefully
- [ ] All integration tests pass

## Tasks

### Basic Flow Tests

1. Create `tests/integration/mcp.test.ts`:
   ```typescript
   describe("MCP tools", () => {
     it("ask_user returns user response", async () => {
       // Mock the modal store response
       const mockEnqueue = vi.fn().mockResolvedValue({ value: "blue" });
       vi.spyOn(useStore.getState(), "enqueue").mockImplementation(mockEnqueue);

       const tool = mcpServer.tools.find(t => t.name === "ask_user");
       const result = await tool!.handler({
         message: "What color?",
         options: ["red", "blue"]
       });

       expect(result.content[0].text).toBe("blue");
       expect(mockEnqueue).toHaveBeenCalledWith({
         type: "ask",
         payload: expect.objectContaining({ message: "What color?" })
       });
     });

     it("confirm_action returns confirmation", async () => {
       // Similar test for confirm flow
     });

     // Tests for request_input and select_option
   });
   ```

### Full E2E Flow Test

2. Create `tests/integration/e2e.test.ts`:
   ```typescript
   describe("End-to-end flow", () => {
     it("sends message and displays response", async () => {
       // Full flow with mocked Claude subprocess
       // 1. User sends message
       // 2. Claude responds with streaming
       // 3. Response appears in viewport
     });

     it("handles tool call with modal interaction", async () => {
       // 1. User sends message
       // 2. Claude calls ask_user tool
       // 3. Modal appears
       // 4. Simulate user response
       // 5. Claude continues with response
     });
   });
   ```

### Concurrent Tool Calls

3. Test concurrent modal handling:
   ```typescript
   it("handles concurrent tool calls", async () => {
     // Claude calls multiple tools
     // Modals queue correctly
     // Each resolves in order
   });
   ```

### Error Cases

4. Test error scenarios:
   ```typescript
   describe("error handling", () => {
     it("handles modal timeout", async () => {
       // Tool call with timeout
       // Timeout triggers
       // Error returned to Claude
     });

     it("handles modal cancellation", async () => {
       // User presses Escape
       // Tool receives cancellation
     });

     it("handles SDK errors", async () => {
       // Network error during query
       // Error displayed to user
     });
   });
   ```

## Files

- `packages/tui/tests/integration/mcp.test.ts` — MCP tool tests
- `packages/tui/tests/integration/e2e.test.ts` — Full flow tests
- `packages/tui/tests/integration/concurrent.test.ts` — Concurrency tests
- `packages/tui/tests/integration/errors.test.ts` — Error handling tests

## Test Scenario Matrix

| Scenario | Tool | User Action | Expected Result |
|----------|------|-------------|-----------------|
| Basic ask | ask_user | Type response | Returns text |
| Ask with options | ask_user | Select option | Returns selected |
| Confirm yes | confirm_action | Press Y | `{confirmed: true}` |
| Confirm no | confirm_action | Press N | `{confirmed: false}` |
| Confirm escape | confirm_action | Press Esc | `{cancelled: true}` |
| Input text | request_input | Type + Enter | Returns text |
| Select option | select_option | Arrow + Enter | Returns selection |
| Timeout | any | Wait | Default/cancel |

## Acceptance Test

```
User: "Ask me what color I like"
Claude: [calls ask_user tool]
[Modal appears with question]
User: [types "blue"]
Claude: "You said you like blue!"
```

## Context

From plan Section 5 (Phase 4 Acceptance Criteria)

---

*Generated from: TUI-TS-INK-CONVERSION-PLAN.md Phase 4*
