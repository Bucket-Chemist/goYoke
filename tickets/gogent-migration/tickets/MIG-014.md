---
id: MIG-014
title: "Migrate TUI DefaultBaseDir() to ~/.gogent/sessions/"
status: pending
dependencies: []
phase: 3
tags: [plan-generated, phase-3, gogent-migration]
needs_planning: false
agent: go-pro
size: M
---

# MIG-014: Migrate TUI DefaultBaseDir() to ~/.gogent/sessions/

## Description

Migrate internal/tui/session/persistence.go DefaultBaseDir() (lines 86-95):

Change line 94 from filepath.Join(home, '.claude', 'sessions') to filepath.Join(home, '.gogent', 'sessions').

Also update CLAUDE_CONFIG_DIR fallback (line 88) — consider adding GOGENT_CONFIG_DIR as an additional env var check. The priority should be: GOGENT_CONFIG_DIR → CLAUDE_CONFIG_DIR → ~/.gogent/sessions.

Update SetupSessionDir (lines 194-233):
- Line 204-206: claudeDir comment references ~/.claude — update to ~/.gogent
- Line 214: comment references .claude/tmp — update to .gogent/tmp
- Line 221: comment references ~/.claude/tmp/ — update to ~/.gogent/tmp/

The actual SetupSessionDir logic uses filepath.Dir(s.baseDir) which automatically resolves correctly once DefaultBaseDir is updated.

Update persistence_test.go:
- Line 175, 262, 285: assertions checking for .claude should check .gogent

Update package doc (line 3): reference ~/.gogent/sessions/ instead of ~/.claude/sessions/.

## Acceptance Criteria

- [ ] DefaultBaseDir returns ~/.gogent/sessions/ by default
- [ ] GOGENT_CONFIG_DIR env var supported
- [ ] SetupSessionDir creates current-session and tmp symlink under ~/.gogent/
- [ ] TUI session tests pass
- [ ] Package documentation updated

## Files

- internal/tui/session/persistence.go -- Target — DefaultBaseDir at line 86, SetupSessionDir comments
- internal/tui/session/persistence_test.go -- Update assertions at lines 175, 262, 285

## Context

Phase 3 of the .claude/ to .gogent/ runtime I/O migration.
Source: Braintrust analysis + Architect specs.

---

_Generated from: .gogent/sessions/20260331-plan-tickets-gogent-migration/specs.md Phase 3_
