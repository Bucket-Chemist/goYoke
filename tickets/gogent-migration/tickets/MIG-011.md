---
id: MIG-011
title: "Migrate cmd/gogent-archive/ to .gogent/ paths"
status: pending
dependencies: [MIG-001]
phase: 3
tags: [plan-generated, phase-3, gogent-migration]
needs_planning: false
agent: go-pro
size: M
---

# MIG-011: Migrate cmd/gogent-archive/ to .gogent/ paths

## Description

Migrate cmd/gogent-archive/main.go — 6 hardcoded .claude/memory/ paths:

1. Line 109: filepath.Join(projectDir, '.claude', 'memory', 'last-handoff.md') → filepath.Join(config.ProjectMemoryDir(projectDir), 'last-handoff.md')

2. Lines 190, 293, 342, 370: filepath.Join(projectDir, '.claude', 'memory', 'handoffs.jsonl') → filepath.Join(config.ProjectMemoryDir(projectDir), 'handoffs.jsonl')

3. Lines 223, 1004: filepath.Join(projectDir, '.claude', 'memory', 'user-intents.jsonl') → filepath.Join(config.ProjectMemoryDir(projectDir), 'user-intents.jsonl')

Also update error messages at lines 298, 347, 375 that reference '.claude/memory/handoffs.jsonl' in user-facing strings.

Add import for config package.

## Acceptance Criteria

- [ ] All .claude/memory/ paths replaced with ProjectMemoryDir calls
- [ ] Error messages reference .gogent/memory/
- [ ] go build ./cmd/gogent-archive/ succeeds
- [ ] No .claude/memory references in production code

## Files

- cmd/gogent-archive/main.go -- Target — 8+ .claude/memory/ references
- pkg/config/paths.go -- ProjectMemoryDir() dependency

## Context

Phase 3 of the .claude/ to .gogent/ runtime I/O migration.
Source: Braintrust analysis + Architect specs.

---

_Generated from: .gogent/sessions/20260331-plan-tickets-gogent-migration/specs.md Phase 3_
