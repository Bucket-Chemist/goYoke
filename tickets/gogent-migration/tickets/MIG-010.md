---
id: MIG-010
title: "Migrate pkg/telemetry/sharp_edge_registry.go (read-only .claude/ → keep)"
status: pending
dependencies: [MIG-001]
phase: 2
tags: [plan-generated, phase-2, gogent-migration]
needs_planning: false
agent: go-pro
size: M
---

# MIG-010: Migrate pkg/telemetry/sharp_edge_registry.go (read-only .claude/ → keep)

## Description

sharp_edge_registry.go lines 29-32 read from .claude/agents/ directory — this is STATIC CONFIG, not runtime state. These paths should NOT change. The .claude/agents/ directory contains agent definitions which are read-only config.

However, update the test files that create .claude/agents/ fixtures:
- sharp_edge_registry_test.go: lines 23, 74, 115 — these create temp .claude/agents/ dirs for testing. These should stay as-is since they test reading from config dir.
- sharp_edge_hit_test.go: line 21 creates .claude/memory/ (change to .gogent/memory/), line 24 creates .claude/agents/ (keep as-is, it's config read).
- review_finding_test.go: line 19 creates .claude/memory/ (change to .gogent/memory/).
- jsonl_writer_test.go: lines 18-20 create .claude/memory/ and .claude/agents/ (change memory to .gogent/memory/, keep agents as-is).

This ticket separates config-read paths (stay .claude/) from state-write paths (migrate to .gogent/).

## Acceptance Criteria

- [ ] sharp_edge_registry.go unchanged (reads from .claude/agents/)
- [ ] .claude/memory/ references in test setup changed to .gogent/memory/
- [ ] .claude/agents/ references in test setup unchanged (config reads)
- [ ] All telemetry tests pass

## Files

- pkg/telemetry/sharp_edge_registry.go -- READ-ONLY .claude/agents/ — no change
- pkg/telemetry/sharp_edge_hit_test.go -- Update memory path, keep agents path
- pkg/telemetry/review_finding_test.go -- Update memory path
- pkg/telemetry/jsonl_writer_test.go -- Update memory path, keep agents path

## Context

Phase 2 of the .claude/ to .gogent/ runtime I/O migration.
Source: Braintrust analysis + Architect specs.

---

_Generated from: .gogent/sessions/20260331-plan-tickets-gogent-migration/specs.md Phase 2_
