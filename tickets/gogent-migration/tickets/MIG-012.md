---
id: MIG-012
title: "Migrate cmd/gogent-sharp-edge/ and cmd/gogent-capture-intent/"
status: pending
dependencies: [MIG-001]
phase: 3
tags: [plan-generated, phase-3, gogent-migration]
needs_planning: false
agent: go-pro
size: M
---

# MIG-012: Migrate cmd/gogent-sharp-edge/ and cmd/gogent-capture-intent/

## Description

Migrate cmd/gogent-sharp-edge/main.go:
- Line 40: filepath.Join(home, '.claude', 'agents') — READ-ONLY config, keep as-is
- Line 126: String referencing '~/.claude/routing-schema.json' — READ-ONLY config, keep as-is
- Line 324: filepath.Join(projectDir, '.claude', 'memory', 'pending-learnings.jsonl') → filepath.Join(config.ProjectMemoryDir(projectDir), 'pending-learnings.jsonl')
- Line 463: String check for '.claude/' path detection — this is used to skip .claude/ files in sharp edge detection. Update to also skip .gogent/ files.

Migrate cmd/gogent-capture-intent/main.go:
- Line 146: filepath.Join(projectDir, '.claude', 'memory', 'user-intents.jsonl') → filepath.Join(config.ProjectMemoryDir(projectDir), 'user-intents.jsonl')

Add config imports.

## Acceptance Criteria

- [ ] pending-learnings write path uses .gogent/memory/
- [ ] user-intents write path uses .gogent/memory/
- [ ] .claude/agents/ read path unchanged
- [ ] Sharp edge filter also skips .gogent/ files
- [ ] Both binaries compile

## Files

- cmd/gogent-sharp-edge/main.go -- Target — line 324 (write), line 463 (filter)
- cmd/gogent-capture-intent/main.go -- Target — line 146
- pkg/config/paths.go -- ProjectMemoryDir() dependency

## Context

Phase 3 of the .claude/ to .gogent/ runtime I/O migration.
Source: Braintrust analysis + Architect specs.

---

_Generated from: .gogent/sessions/20260331-plan-tickets-gogent-migration/specs.md Phase 3_
