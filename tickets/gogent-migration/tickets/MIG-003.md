---
id: MIG-003
title: "Migrate pkg/session/session_dir.go to .gogent/"
status: pending
dependencies: [MIG-001]
phase: 2
tags: [plan-generated, phase-2, gogent-migration]
needs_planning: false
agent: go-pro
size: M
---

# MIG-003: Migrate pkg/session/session_dir.go to .gogent/

## Description

Migrate all three functions in pkg/session/session_dir.go to use .gogent/ instead of .claude/:

1. CreateSessionDir (line 14-25): Change line 19 from filepath.Join(projectDir, '.claude', 'sessions', sessionID) to filepath.Join(config.RuntimeDir(projectDir), 'sessions', sessionID). Add import for config package.

2. WriteCurrentSession (line 28-40): Change line 29 from filepath.Join(projectDir, '.claude') to config.RuntimeDir(projectDir). Change line 34 accordingly. The current-session marker moves to .gogent/current-session.

3. ReadCurrentSession (line 44-54): Change line 45 from filepath.Join(projectDir, '.claude', 'current-session') to filepath.Join(config.RuntimeDir(projectDir), 'current-session').

Update session_dir_test.go to expect .gogent/ paths:
- Line 20: expected .gogent/sessions/ instead of .claude/sessions/
- Line 70, 76: .gogent/ paths for current-session
- Line 83, 105: similarly

Comments and doc strings referencing .claude/ must also be updated.

## Acceptance Criteria

- [ ] CreateSessionDir creates under .gogent/sessions/
- [ ] WriteCurrentSession writes to .gogent/current-session
- [ ] ReadCurrentSession reads from .gogent/current-session
- [ ] All tests in session_dir_test.go pass
- [ ] No .claude references remain in session_dir.go

## Files

- pkg/session/session_dir.go -- Target — 3 functions to migrate
- pkg/session/session_dir_test.go -- Update test expectations
- pkg/config/paths.go -- RuntimeDir() dependency

## Context

Phase 2 of the .claude/ to .gogent/ runtime I/O migration.
Source: Braintrust analysis + Architect specs.

---

_Generated from: .gogent/sessions/20260331-plan-tickets-gogent-migration/specs.md Phase 2_
