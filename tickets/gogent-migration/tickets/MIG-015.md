---
id: MIG-015
title: "Migrate pkg/routing/ read paths (identity_loader, schema, agents)"
status: pending
dependencies: [MIG-001, MIG-003]
phase: 3
tags: [plan-generated, phase-3, gogent-migration]
needs_planning: false
agent: go-pro
size: M
---

# MIG-015: Migrate pkg/routing/ read paths (identity_loader, schema, agents)

## Description

These files READ from .claude/ (static config) — most paths should NOT change. However, verify and update:

pkg/routing/identity_loader.go:
- Lines 127-129: Reads .claude/current-session — this moves to .gogent/current-session. Change filepath.Join(projectDir, '.claude', 'current-session') to filepath.Join(config.RuntimeDir(projectDir), 'current-session'). Update error message at line 129.
- Lines 21, 139-141: Comments referencing ~/.claude/agents/ — keep as-is (config read)

pkg/routing/schema.go:
- Line 357: Reads .claude/routing-schema.json — keep as-is (config read)
- Line 373: Reads .claude/routing-schema.json — keep as-is
- schema_test.go line 72: References .claude/tmp/max_delegation — this is an XDG path, already correct

pkg/routing/agents.go:
- Line 170, 186: Reads .claude/agents/agents-index.json — keep as-is (config read)

pkg/telemetry/cost.go:
- Line 74: Reads ~/.claude/routing-schema.json — keep as-is (config read)

Update identity_loader_test.go: Lines 327-522 extensively create .claude/sessions/ and .claude/current-session fixtures. These must change to .gogent/ paths for session/current-session references while keeping .claude/ for agent identity reads.

## Acceptance Criteria

- [ ] identity_loader reads current-session from .gogent/
- [ ] Config read paths (.claude/agents/, .claude/routing-schema.json) unchanged
- [ ] identity_loader_test.go fixture paths updated
- [ ] All routing tests pass

## Files

- pkg/routing/identity_loader.go -- Target — line 127 current-session read
- pkg/routing/identity_loader_test.go -- Update session fixture paths (lines 327-522)
- pkg/routing/schema.go -- Verify — no changes needed (config read)
- pkg/routing/agents.go -- Verify — no changes needed (config read)
- pkg/config/paths.go -- RuntimeDir() dependency

## Context

Phase 3 of the .claude/ to .gogent/ runtime I/O migration.
Source: Braintrust analysis + Architect specs.

---

_Generated from: .gogent/sessions/20260331-plan-tickets-gogent-migration/specs.md Phase 3_
