---
id: MIG-006
title: "Migrate pkg/session/attention_gate.go and context_loader.go to .gogent/"
status: pending
dependencies: [MIG-001]
phase: 2
tags: [plan-generated, phase-2, gogent-migration]
needs_planning: false
agent: go-pro
size: M
---

# MIG-006: Migrate pkg/session/attention_gate.go and context_loader.go to .gogent/

## Description

Migrate attention_gate.go paths (2 references):

1. CountPendingLearnings (line 69): Change filepath.Join(projectDir, '.claude', 'memory', 'pending-learnings.jsonl') to filepath.Join(config.ProjectMemoryDir(projectDir), 'pending-learnings.jsonl')

2. ArchivePendingLearnings (lines 98-99): Change both paths:
   - pendingPath: use ProjectMemoryDir
   - sharpEdgesDir: filepath.Join(config.ProjectMemoryDir(projectDir), 'sharp-edges')

Migrate context_loader.go paths (2 references):

1. LoadHandoffSummary (line 16): Change filepath.Join(projectDir, '.claude', 'memory', 'last-handoff.md') to filepath.Join(config.ProjectMemoryDir(projectDir), 'last-handoff.md')

2. CheckPendingLearnings (line 71): Change filepath.Join(projectDir, '.claude', 'memory', 'pending-learnings.jsonl') to use ProjectMemoryDir

Add config import to both files. No test file changes needed for attention_gate — existing tests use injected paths. For context_loader, update context_response_test.go references if any hardcode .claude/ paths (lines 152, 173, 181-182, 222, 246).

## Acceptance Criteria

- [ ] attention_gate.go uses .gogent/memory/ paths
- [ ] context_loader.go uses .gogent/memory/ paths
- [ ] All session package tests pass
- [ ] No .claude/memory references in either file

## Files

- pkg/session/attention_gate.go -- Target — 2 path references
- pkg/session/context_loader.go -- Target — 2 path references
- pkg/session/context_response_test.go -- Update hardcoded .claude/ paths in test data
- pkg/config/paths.go -- ProjectMemoryDir() dependency

## Context

Phase 2 of the .claude/ to .gogent/ runtime I/O migration.
Source: Braintrust analysis + Architect specs.

---

_Generated from: .gogent/sessions/20260331-plan-tickets-gogent-migration/specs.md Phase 2_
