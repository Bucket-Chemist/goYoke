---
id: MIG-001
title: "Implement RuntimeDir() and ProjectMemoryDir() in pkg/config/paths.go"
status: pending
dependencies: []
phase: 1
tags: [plan-generated, phase-1, gogent-migration]
needs_planning: false
agent: go-pro
size: M
---

# MIG-001: Implement RuntimeDir() and ProjectMemoryDir() in pkg/config/paths.go

## Description

Add two new functions to pkg/config/paths.go:

1. `RuntimeDir(projectDir string) string` — returns filepath.Join(projectDir, ".gogent"). Uses sync.Once to create the directory on first call. Supports GOGENT_RUNTIME_DIR env var override.

2. `ProjectMemoryDir(projectDir string) string` — returns filepath.Join(RuntimeDir(projectDir), "memory"). Creates directory on first call.

Also normalize GetProjectViolationsLogPath (line 82-84) to use ProjectMemoryDir instead of hardcoded .claude/memory path. The function currently returns filepath.Join(projectDir, ".claude", "memory", "routing-violations.jsonl") — change to filepath.Join(ProjectMemoryDir(projectDir), "routing-violations.jsonl").

Add unit tests in pkg/config/paths_test.go:
- RuntimeDir returns {projectDir}/.gogent/
- RuntimeDir respects GOGENT_RUNTIME_DIR env var
- ProjectMemoryDir returns {projectDir}/.gogent/memory/
- GetProjectViolationsLogPath returns .gogent/memory/routing-violations.jsonl
- Directory creation is idempotent (call twice, no error)

Pattern to follow: existing WithProjectDir functions at lines 294-390.

## Acceptance Criteria

- [ ] RuntimeDir(projectDir) returns filepath.Join(projectDir, '.gogent')
- [ ] GOGENT_RUNTIME_DIR env var overrides the default
- [ ] ProjectMemoryDir creates .gogent/memory/ subdirectory
- [ ] GetProjectViolationsLogPath uses ProjectMemoryDir instead of .claude/memory
- [ ] sync.Once ensures directory creation happens only once
- [ ] All new functions have tests

## Files

- pkg/config/paths.go -- Target file — add RuntimeDir/ProjectMemoryDir, fix GetProjectViolationsLogPath at line 82
- pkg/config/paths_test.go -- Add tests for new functions

## Context

Phase 1 of the .claude/ to .gogent/ runtime I/O migration.
Source: Braintrust analysis + Architect specs.

---

_Generated from: .gogent/sessions/20260331-plan-tickets-gogent-migration/specs.md Phase 1_
