# TC-001: Replace `--permission-mode delegate` with `--allowedTools`

**Phase**: Phase 0: Foundation Fixes

**Priority**: CRITICAL

**Blocked By**: none

**Effort**: 0.5 days (design decision + documentation; implementation is part of TC-008)

## Description

The Go binary's `spawnAndWait()` function uses `--permission-mode delegate` to grant tool access to spawned agents. Evidence from `docs/PERMISSION_HANDLING.md` and `spawnAgent.ts` shows this flag is insufficient in pipe mode. The working pattern is `--allowedTools "Read,Write,Glob,Grep,Bash,Edit"`, as already implemented in `spawnAgent.ts:buildCliArgs()`.

Document the correct CLI arg pattern for the Go binary. Keep `--permission-mode delegate` as belt-and-suspenders but do not rely on it. The actual implementation happens in TC-008 (Go binary), but the design decision must be made here so TC-008 has the correct specification.

## Implementation Instructions

### Step 1: Create Permission Design Document

Create `/home/doktersmol/Documents/GOgent-Fortress/tickets/team-coordination/PERMISSION-DESIGN.md` with:

```markdown
# Permission Handling for Team-Spawned Agents

## Core Decision

**Primary mechanism**: `--allowedTools` flag (comma-separated list)
**Fallback**: `--permission-mode delegate` (belt-and-suspenders, do not rely on it)

## Evidence

From `docs/PERMISSION_HANDLING.md`:
- `--permission-mode delegate` does NOT work in pipe mode (`-p` flag)
- Permission events in pipe mode are DENIALS, not REQUESTS
- No response protocol exists to approve denied tools
- `--allowedTools` is the intended mechanism for automation

From `spawnAgent.ts:buildCliArgs()` (lines 323-346):
- Line 335: `--permission-mode delegate` included
- Lines 337-339: `--allowedTools` is the working implementation
- Line 351-371: `parseCliOutput()` handles cost extraction

## Per-Agent Tool Lists

| Agent Type | Allowed Tools | Rationale |
|------------|---------------|-----------|
| **Analysis agents** (einstein, staff-arch, beethoven) | Read, Glob, Grep | Read-only analysis, no file modification |
| **Review agents** (backend-reviewer, frontend-reviewer) | Read, Glob, Grep | Code review is read-only |
| **Implementation agents** (worker agents in impl workflow) | Read, Write, Edit, Glob, Grep, Bash | Full implementation cycle |
| **Orchestrators** (mozart) | Read, Glob, Grep | Coordination only, no direct implementation |

## CLI Invocation Pattern

```bash
# Correct pattern (copied from spawnAgent.ts)
claude -p --output-format json \
  --model opus \
  --permission-mode delegate \
  --allowedTools Read,Write,Glob,Grep,Bash,Edit \
  --max-budget-usd 5.0
```

## Integration with agents-index.json

TC-014 will add `allowed_tools` field to agents-index.json:

```json
{
  "agents": [
    {
      "id": "einstein",
      "allowed_tools": ["Read", "Glob", "Grep"]
    }
  ]
}
```

The Go binary will read this field and construct `--allowedTools` accordingly.
```

### Step 2: Document CLI Flag Verification Test

Add to `PERMISSION-DESIGN.md`:

```markdown
## Manual Verification Test

Before Phase 2 implementation, verify the flag pattern works:

```bash
# Test 1: Verify tools work in pipe mode
echo "Create /tmp/test-gogent.txt with content 'hello'" | \
  claude -p --output-format json \
  --permission-mode delegate \
  --allowedTools Write

# Expected: File created successfully, no permission denial
# Actual result: [FILL IN DURING PHASE 0]

# Test 2: Verify Read tool
echo "Read /etc/hostname" | \
  claude -p --output-format json \
  --permission-mode delegate \
  --allowedTools Read

# Expected: Hostname returned in JSON output
# Actual result: [FILL IN DURING PHASE 0]
```

**Quality Gate for Phase 2**: Both tests must pass before implementing TC-008.
```

### Step 3: Update Implementation Plan Reference

Add section to `IMPLEMENTATION-PLAN-FINAL.md` (or reference PERMISSION-DESIGN.md from TC-008).

## Code Guidelines

- **Flag order**: `--permission-mode delegate` BEFORE `--allowedTools` (matches spawnAgent.ts pattern)
- **Tool names**: Exact case-sensitive match: `Read`, `Write`, `Edit`, `Bash`, `Glob`, `Grep`
- **Comma-separated**: No spaces: `Read,Write,Glob` (not `Read, Write, Glob`)
- **Error handling**: If `allowed_tools` field is missing from agents-index.json, use conservative default: `["Read", "Glob", "Grep"]`

Reference `spawnAgent.ts:buildCliArgs()` as the canonical implementation.

## Success Criteria

- [x] `PERMISSION-DESIGN.md` created with per-agent tool lists
- [x] Manual verification test documented
- [ ] Manual verification test executed (both Test 1 and Test 2 pass)
- [ ] CLI flag pattern matches `spawnAgent.ts:buildCliArgs()` exactly
- [ ] Per-agent tool lists documented for all agent types in braintrust, review, implementation workflows
- [ ] Quality gate added to TC-008: "Agent spawned via `claude -p` can successfully use Write tool"

## Test Coverage Requirements

### Unit Tests (in TC-008)

Since this is a design ticket, actual tests are in TC-008. Document required test cases here:

1. **Test: buildCLIArgs() with allowed_tools from config**
   - Input: Agent config with `allowed_tools: ["Read", "Write"]`
   - Expected: CLI args contain `--allowedTools Read,Write`
   - Assertion: String slice contains exactly `["--allowedTools", "Read,Write"]`

2. **Test: buildCLIArgs() with missing allowed_tools**
   - Input: Agent config without `allowed_tools` field
   - Expected: CLI args contain `--allowedTools Read,Glob,Grep` (conservative default)
   - Assertion: Fallback list is used

3. **Test: buildCLIArgs() with empty allowed_tools**
   - Input: Agent config with `allowed_tools: []`
   - Expected: CLI args omit `--allowedTools` flag entirely (no tools allowed)
   - Edge case: Deliberate lockdown

### Integration Tests (in TC-008)

1. **Test: Spawned agent can use Write tool**
   - Spawn test agent with `allowed_tools: ["Write"]`
   - Agent prompt: "Write /tmp/gogent-test-write.txt with content 'success'"
   - Assertion: File exists and contains "success"
   - Cleanup: Delete file after test

2. **Test: Spawned agent cannot use Bash if not in allowed_tools**
   - Spawn test agent with `allowed_tools: ["Read"]`
   - Agent prompt: "Run command `echo hello`"
   - Expected: Permission denial in stderr
   - Assertion: Exit code != 0 OR output contains "permission" error

## Files to Create/Modify

| File | Action | Purpose |
|------|--------|---------|
| `/home/doktersmol/Documents/GOgent-Fortress/tickets/team-coordination/PERMISSION-DESIGN.md` | Create | Permission handling specification |
| `/home/doktersmol/Documents/GOgent-Fortress/tickets/team-coordination/IMPLEMENTATION-PLAN-FINAL.md` | Modify | Reference PERMISSION-DESIGN.md from TC-008 section |
| `/tmp/test-gogent.txt` | Create (temp) | Manual verification artifact |

## Dependencies and Integration Points

### Feeds Into
- **TC-008** (Go binary implementation): Uses this design for `buildCLIArgs()` function
- **TC-014** (`cli_flags` in agents-index.json): Per-agent tool lists come from this ticket

### References
- `spawnAgent.ts:buildCliArgs()` (lines 323-346): Canonical implementation
- `docs/PERMISSION_HANDLING.md`: Evidence that `--permission-mode delegate` is insufficient
- `spawnAgent.ts:parseCliOutput()` (lines 351-371): Cost extraction pattern

## Acceptance Checklist

- [ ] PERMISSION-DESIGN.md created with all 4 agent categories documented
- [ ] CLI flag pattern matches spawnAgent.ts exactly (verified by side-by-side diff)
- [ ] Manual Test 1 executed and passed (Write tool works)
- [ ] Manual Test 2 executed and passed (Read tool works)
- [ ] Per-agent tool lists defined for braintrust workflow (einstein, staff-arch, beethoven, mozart)
- [ ] Per-agent tool lists defined for review workflow (4 reviewers)
- [ ] Per-agent tool lists defined for implementation workflow (worker agents)
- [ ] Conservative fallback defined for missing `allowed_tools` field
- [ ] Quality gate added to TC-008 acceptance criteria
- [ ] TC-014 ticket updated to reference this design

**Source**: Staff-Architect C-1, Assumption A-1 (CRITICAL)
