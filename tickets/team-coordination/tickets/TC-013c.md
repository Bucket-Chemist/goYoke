# TC-013c: Implementation Workflow Team-Run Migration

**Phase**: Phase 4c (third of 3 decomposed from TC-013)
**Priority**: HIGH
**Blocked By**: TC-013b (braintrust validates multi-wave pattern)
**Effort**: 2-3 days
**Status**: in_progress

---

## Description

Migrate the `/ticket` implementation workflow from foreground sequential agent spawning to background `gogent-team-run` execution with computed wave DAGs.

**Current behavior:** Router invokes impl-manager via `Task(sonnet)`, which reads specs.md, builds a task DAG, and spawns worker agents (go-pro, python-pro, etc.) sequentially via `Task()`. TUI freezes for 5-15 minutes.

**Target behavior:** Router reads specs.md, invokes `gogent-plan-impl` (Go binary) to parse tasks and compute waves via Kahn's algorithm, generates config.json + stdin files, launches `gogent-team-run`. TUI returns in <15 seconds.

**Key challenges:**
- Dynamic member count (variable number of tasks from implementation-plan.json)
- Variable wave count (depends on dependency graph)
- Requires new Go binary `gogent-plan-impl` (consolidated from TC-013's 4 proposed binaries)

**Reference:** `tickets/team-coordination/ImplMgr-team-bridge.md` (authoritative corrections)

### Phase 0 Prerequisites (Completed)

- [x] D-1: `description` field added to `worker.json` schema for envelope builder compatibility
- [x] D-3: Wave failure propagation scoped as deliverable (see below)

### Phase 0.5: Architect Dual-Output (BLOCKER)

**Decision:** Architect agent outputs BOTH `specs.md` (human review) AND `implementation-plan.json` (machine automation). This eliminates the fragile specs.md markdown parser from `gogent-plan-impl`. The Go binary reads validated JSON instead.

**Staff-Architect Review:** APPROVED WITH CONDITIONS. 12 findings addressed. See `plans/streamed-wandering-wilkes.md`.

- [x] Create `schemas/architect/implementation-plan.json` schema (version 1.0.0, task_id pattern `^task-\d{3}$`, coverage_target field)
- [x] Modify `architect.md` — add Output 3 with embedded schema, 2-task example, ordering instruction (JSON first)
- [x] Update `agents-index.json`: add output_artifacts for implementation-plan.json, Write to cli_flags
- [ ] Test: architect produces valid JSON for a sample task
- [x] Update TC-013c: reference implementation-plan.json as input to gogent-plan-impl (this item)

**Impact on Implementation Instructions below:**
- Section 1 (`gogent-plan-impl`): reads `implementation-plan.json` (JSON), NOT `specs.md` (markdown). No markdown parser needed.
- Section 2 (specs.md parsing): REPLACED by JSON unmarshalling + validation
- Sections 3-8: unchanged (wave computation, stdin generation, config generation, launch, fallback, feature flag)

---

## Prerequisites

- [x] TC-013-prereq: Binary bugs fixed
- [x] TC-020: Bridge docs completed
- [x] TC-013a: Review validates team-run pattern
- [x] TC-013a stdout format compliance: `buildPromptEnvelope()` embeds stdout contract schema automatically for any workflow with a matching schema in `schemas/teams/stdin-stdout/`
- [ ] TC-013b: Braintrust validates multi-wave + inter-wave pattern

---

### Stdout Compliance Architecture (from TC-013a)

Worker agent stdout compliance is handled automatically by the envelope builder — no additional work needed in TC-013c beyond ensuring the contract schema exists.

**How it works:**
1. `buildPromptEnvelope(teamDir, member, workflowType)` calls `resolveStdoutSchema("implementation", agentID)`
2. For implementation workers (go-pro, python-pro, etc.), the candidate lookup tries: `implementation-go-pro.json` → `implementation-go.json` → `implementation-worker.json`
3. The generic `implementation-worker.json` contract schema is matched (already exists at `schemas/teams/stdin-stdout/implementation-worker.json`)
4. The `stdout` section is embedded in the prompt with format instructions
5. `writeStdoutFile()` tier-2 extracts the ```json code block from the agent's response

**What TC-013c must verify:**
- `implementation-worker.json` has a meaningful `stdout` section (check that it defines fields like `task_id`, `status`, `files_modified`, `tests_passed`, etc.)
- If it doesn't have a `stdout` key yet, add one — otherwise agents get no output format instructions and fall back to markdown
- Generated `config.json` must set `workflow_type: "implementation"` for the schema resolution to work

---

## Implementation Instructions

### 1. Build `gogent-plan-impl` Go Binary

Single binary that replaces TC-013's 4 proposed binaries (`gogent-team-init-impl`, `gogent-parse-specs`, `gogent-compute-waves`, plus config generation).

```
cmd/gogent-plan-impl/main.go

Usage: gogent-plan-impl --plan=<path> --project-root=<path> --output=<team-dir>

The binary:
1. Reads implementation-plan.json (NOT specs.md — Phase 0.5 eliminated markdown parsing)
2. Validates version, task_id format, required fields
3. Validates that referenced agents exist in `agents-index.json` (fail early with clear error if unknown agent)
4. Validates blocked_by referential integrity (all referenced task_ids exist)
5. Computes wave DAG via Kahn's algorithm (topological sort)
6. Generates config.json from schemas/teams/implementation.json template
7. Generates stdin files per worker (compliant with schemas/stdin/worker.json)
8. Validates generated stdin files against `schemas/stdin/worker.json` before writing
9. Writes all files to --output directory

Exit codes: 0=success, 1=JSON read/parse error, 2=schema validation, 3=referential integrity
```

### 2. implementation-plan.json Input (replaces specs.md parsing)

**Phase 0.5 replaced markdown parsing with JSON input.** The architect agent now produces `implementation-plan.json` alongside `specs.md`. The Go binary reads JSON via `json.Unmarshal()` — no markdown parser needed.

**Schema:** `schemas/architect/implementation-plan.json` (version 1.0.0)

**Field mapping (implementation-plan.json → worker.json stdin):**

| implementation-plan.json | worker.json stdin | Source |
|--------------------------|-------------------|--------|
| `tasks[i].agent` | `agent` | Direct |
| (constant) | `workflow`: `"implementation"` | Hardcoded |
| (runtime) | `context.project_root` | --project-root flag |
| (runtime) | `context.team_dir` | Computed |
| `tasks[i].task_id` | `task.task_id` | Direct |
| `tasks[i].subject` | `task.subject` | Direct |
| `tasks[i].description` | `task.description` | Direct |
| `tasks[i].acceptance_criteria` | `task.acceptance_criteria` | Direct |
| `tasks[i].blocked_by` | `task.blocked_by` | Direct |
| (computed) | `task.blocks` | Reverse-lookup from all tasks' blocked_by |
| `tasks[i].target_packages` | `implementation_scope.target_packages` | Direct |
| `tasks[i].related_files` | `implementation_scope.related_files` | Direct |
| `tasks[i].tests_required` | `implementation_scope.tests_required` | Direct |
| `tasks[i].coverage_target` | `implementation_scope.coverage_target` | Direct (optional) |
| `project.build_verification` | `implementation_scope.build_verification` | Shared |
| `project.language` | `conventions.language` | Shared |
| `project.conventions_file` | `conventions.conventions_file` | Shared |
| `project.error_handling` | `conventions.error_handling` | Shared |
| `project.test_pattern` | `conventions.test_pattern` | Shared |
| `project.architecture_notes` | `codebase_context.architecture_notes` | Shared |
| `project.patterns_to_follow` | `codebase_context.patterns_to_follow` | Shared |
| `project.anti_patterns` | `codebase_context.anti_patterns` | Shared |
| `"Implement: " + subject` | `description` | Computed (envelope builder) |

**Agent-to-convention mapping (for validation + inference):**

| Agent ID | language | conventions_file |
|----------|----------|------------------|
| go-pro, go-cli, go-tui, go-api, go-concurrent | go | go.md |
| python-pro, python-ux | python | python.md |
| typescript-pro, react-pro | typescript | typescript.md |
| r-pro, r-shiny-pro | r | R.md |

**Validation error format (structured stderr):**
```
Error: [category] [detail]
  at: tasks[2].blocked_by[0]
  value: "task-999"
  expected: one of [task-001, task-002, task-003]
```

**Binary source files (simplified from original plan):**
```
cmd/gogent-plan-impl/
  main.go       — CLI entry: read JSON, validate, compute waves, generate
  validate.go   — Schema + referential integrity + agent validation
  waves.go      — Kahn's algorithm
  generator.go  — config.json + stdin generation
  main_test.go  — Tests (happy path + malformed JSON)
```

`parser.go` (markdown parser) is NOT needed.

### 3. Wave Computation (Kahn's Algorithm)

```
Input: tasks with blocked_by relationships
Output: waves where wave N+1 only contains tasks whose dependencies are all in waves 0..N

Algorithm:
1. Compute in-degree for each task
2. Wave 0: all tasks with in-degree 0
3. Remove Wave 0, decrement dependents' in-degrees
4. Wave 1: all tasks with in-degree 0
5. Repeat until empty
6. If tasks remain with in-degree > 0: circular dependency error
```

Edge cases:
- A->B->C: [[A], [B], [C]]
- A, B (no deps): [[A, B]]
- A->C, B->C, C->D: [[A, B], [C], [D]]
- A->B, B->A: Error: "circular dependency among: A, B"
- A->X (X not in list): Error: "unknown dependency X referenced by A"

### 4. Stdin File Generation

Each task gets a stdin file compliant with `schemas/stdin/worker.json`. Required fields:
- `agent`: dynamic (from specs.md task agent field)
- `workflow`: `"implementation"` (const)
- `context`: `{project_root, team_dir}` (absolute paths)
- `task`: `{task_id, subject, description, acceptance_criteria, blocked_by, blocks}` — NOTE: this is an **object**, not a string. The envelope builder uses the separate `description` field (string) for validation.
- `implementation_scope`: `{target_packages, related_files, tests_required}`
- `conventions`: `{language, conventions_file}` (inferred from agent type)
- `codebase_context`: `{architecture_notes, patterns_to_follow, anti_patterns}`
- `description`: string — **required by envelope builder**. Use format: `"Implement: {task.subject}"`

See `ImplMgr-team-bridge.md` Section 4 for complete validated example.

### 5. Config.json Generation

From `schemas/teams/implementation.json` template:
- `waves[]`: computed by Kahn's algorithm
- `waves[].members[]`: tasks assigned to each wave
- `wave_number`: 1-indexed (per template convention)
- `on_complete_script`: null for implementation waves

**Team directory:** Create under `$GOGENT_SESSION_DIR/teams/{timestamp}.implementation/` (uses `GOGENT_SESSION_DIR` env var set by TUI, matching TC-013a/b pattern).

### 6. Launch Sequence

```bash
# 1. Parse and generate
gogent-plan-impl \
  --plan=".claude/tmp/implementation-plan.json" \
  --project-root="$(pwd)" \
  --output="$team_dir"

# 2. Launch
gogent-team-run "$team_dir"

# 3. Verify
sleep 2
background_pid=$(jq -r '.background_pid' "$team_dir/config.json")
# ... standard verification
```

### 7. Fallback for Simple Tickets

If ticket has no specs.md, fall back to current behavior (direct delegation to language agent, no team pattern). The team pattern is only used when specs.md exists with multiple tasks.

### 8. Feature Flag

Same as TC-013a/b: `settings.json -> "use_team_pattern": true/false`.

---

## Files to Create/Modify

| File | Action | Purpose |
|------|--------|---------|
| `cmd/gogent-plan-impl/main.go` | **Create** | CLI entry: read JSON, validate, compute waves, generate |
| `cmd/gogent-plan-impl/validate.go` | **Create** | Schema + referential integrity + agent validation |
| `cmd/gogent-plan-impl/waves.go` | **Create** | Kahn's algorithm for topological sort |
| `cmd/gogent-plan-impl/generator.go` | **Create** | config.json + stdin file generation |
| `cmd/gogent-plan-impl/main_test.go` | **Create** | Unit tests (happy path + malformed JSON) |
| `cmd/gogent-team-run/wave.go` | **Modify** | Add wave failure propagation (D-3) |
| `cmd/gogent-team-run/wave_test.go` | **Modify** | Add test for failure propagation behavior |
| `~/.claude/skills/ticket/SKILL.md` | **Modify** | Add team-run dispatch when specs.md present |

---

## Success Criteria

- [x] `gogent-plan-impl` binary compiles and passes tests (989 LoC, 11 tests, all pass)
- [x] Circular dependencies detected with clear error message (TestComputeWaves_CircularDependency)
- [x] Unknown dependencies detected with clear error message (TestValidatePlan_UnknownDependency)
- [ ] TUI returns to user within 15 seconds (requires e2e test)
- [ ] Waves execute sequentially (Wave N+1 waits for Wave N) (requires e2e test)
- [ ] Members within waves run in parallel (verified by timestamps) (requires e2e test)
- [ ] At least 2 successful runs with different DAG structures (requires e2e test)
- [x] Fallback to direct delegation when no specs.md (SKILL.md Phase 5.5 decision logic)
- [x] Feature flag toggles between team-run and foreground (SKILL.md reads use_team_pattern from settings.json)
- [x] Wave failure propagation: failed Wave N → subsequent wave members get `status: skipped` (TestRunWaves_FailurePropagation)
- [x] Generated worker stdin files include `description` field (envelope builder requirement) (TestGenerateStdinFile_BlocksComputation)
- [ ] Generated worker stdin files pass validation against `schemas/stdin/worker.json` (runtime validation not yet wired)
- [x] Agent validation: `gogent-plan-impl` rejects unknown agent names with clear error (TestValidatePlan_UnknownAgent)
- [ ] Schema validation: `gogent-plan-impl` validates generated stdin against `schemas/stdin/worker.json` before writing (TODO: add JSON schema validation)
- [x] Regression: wave failure propagation in `wave.go` does NOT break existing review workflow (TestRunWaves_AllSuccessNoSkip + all 57 existing tests pass)
- [x] Worker stdout files contain structured JSON (NOT raw markdown). Mechanism: `resolveStdoutSchema("implementation", agentID)` → resolves `implementation-worker.json` via worker fallback → embeds stdout schema in prompt (implementation-worker.json already has stdout section)
- [x] `implementation-worker.json` has a `stdout` section with fields appropriate for implementation tasks (task_id, status, files_modified, tests_passed, errors) (verified: worker, task_id, status, summary, files_modified, tests_written, acceptance_criteria_met, build_status, blockers, metadata)
- [x] Generated `config.json` sets `workflow_type: "implementation"` (TestGenerateConfig_WaveStructure)

---

## Assumptions to Validate

| Assumption | How to Validate | When |
|------------|-----------------|------|
| Wave failure propagation doesn't break review workflow | Run TC-013a review workflow after `wave.go` modification; verify single-wave review still completes normally | After wave.go changes, before e2e testing |
| Architect reliably produces valid implementation-plan.json | Spawn architect with sample task, validate JSON output against schema (Phase 0.5 test) | Before gogent-plan-impl implementation |
| `gogent-plan-impl` schema validation catches all envelope builder requirements | Generate stdin with `description` omitted, verify validation rejects it | Unit tests |
| blocked_by references are internally consistent | `gogent-plan-impl` validates all blocked_by task_ids exist in tasks array | Unit tests |

---

## Test Cases

### Test 1: Multi-wave DAG (5 tasks, 3 waves)
- implementation-plan.json: 5 tasks with dependency chain
- Wave 1: 2 tasks (no deps), Wave 2: 1 task, Wave 3: 2 tasks
- All complete successfully

### Test 2: Circular dependency detection
- implementation-plan.json: task-001 blocked_by task-002, task-002 blocked_by task-001
- `gogent-plan-impl` errors: "circular dependency detected"
- No team launched

### Test 3: Single-task (no specs.md)
- Ticket has no specs.md
- Falls back to direct delegation (no team pattern)

### Test 4: Task failure mid-wave (requires wave.go modification)
- Wave 1 has 2 tasks, one fails after retries
- **Expected behavior:** After Wave 1 completes, `runWaves` checks member statuses. If any member has `status: failed`, subsequent waves are skipped and their members are set to `status: skipped`.
- Config.json shows: Wave 1 member with `status: failed`, Wave 2 members with `status: skipped`
- **Implementation:** Modify `runWaves()` in `wave.go` to check member statuses after `wg.Wait()` and before proceeding to next wave. Currently `runWaves` iterates unconditionally.
- **Test file:** Add test to `wave_test.go` that spawns a wave with a deliberately failing member and verifies Wave 2 members get `status: skipped`

### Test 5: Unknown dependency
- implementation-plan.json references non-existent task in blocked_by
- `gogent-plan-impl` errors: "unknown dependency"

---

## Notes

This is the most complex of the three decomposed tickets. The `gogent-plan-impl` binary is the main deliverable — it consolidates TC-013's originally proposed 4 binaries into 1.

Phase 0.5 replaced the fragile specs.md markdown parser with structured JSON input (`implementation-plan.json`). The architect agent now produces both specs.md (human review) and implementation-plan.json (machine automation). The Go binary reads validated JSON via `json.Unmarshal()` — no markdown parsing needed.

Agent validation against `agents-index.json` is included in step 2 of the binary spec (promoted from recommendation to requirement per braintrust analysis).

**Staff-Architect Review:** Full review at `plans/streamed-wandering-wilkes.md`. 12 findings (3 HIGH, 6 MEDIUM, 3 LOW), all addressed.
