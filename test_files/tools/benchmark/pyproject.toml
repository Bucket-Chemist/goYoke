[project]
name = "gogent-benchmark"
version = "0.1.0"
description = "Harbor adapter for GOgent-Fortress agent benchmarking"
requires-python = ">=3.12"

[project.optional-dependencies]
dev = ["pytest"]
