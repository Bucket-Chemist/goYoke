---
name: sandbox
description: "[ARCHIVED] Sandbox write tool was provided by goyoke-standalone MCP server, now archived."
trigger: sandbox block, sensitive file, write blocked
---

# /sandbox

**Status: Archived**

The `sandbox_write` and `sandbox_status` tools were provided by the `goyoke-standalone` MCP server,
which has been archived. To write to protected `.claude/` paths, approve CC's permission dialog when prompted.
