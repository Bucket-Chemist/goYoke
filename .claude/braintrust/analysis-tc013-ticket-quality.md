# Braintrust Analysis: TC-013a/b/c Ticket Quality Review

> **Generated by Beethoven**
> **Timestamp**: 2026-02-08T17:30:00Z
> **Session**: braintrust-tc013abc-quality
> **Problem Brief**: TC-013a/b/c ticket specificity and backend integration completeness
> **Prior Analysis**: `analysis-tc013-alignment-review.md` (2026-02-08T12:00:00Z)

---

## Known Gaps in This Document (Added by Mozart post-synthesis)

This document is ~85% actionable for a cold-start session. Before executing Phase 0 decisions, the following gaps must be resolved:

### Gap 1: D-1 schema edits are not diff-ready
The document says "add `description` as optional field to all stdin schemas" but does not specify the exact edit. All stdin schemas (`schemas/stdin/reviewer.json`, `einstein.json`, `beethoven.json`, `worker.json`) have `"additionalProperties": false`. You MUST:
1. Add `"description": {"type": "string", "description": "Human-readable task summary for envelope builder compatibility"}` to the `properties` block of each schema
2. Do NOT add `description` to the `required` array (it must be optional)
3. The `additionalProperties: false` can stay since `description` is now in `properties`
4. Use these description strings when generating stdin files:
   - Reviewer: `"Review {domain} code changes for {focus_areas}"`
   - Einstein: `"Theoretical analysis: {problem_brief.title}"`
   - Beethoven: `"Synthesize analyses for: {problem_brief.title}"`
   - Worker: `"Implement: {task.subject}"`

### Gap 2: D-5 Beethoven schema diff not provided
`schemas/stdin/beethoven.json` needs concrete modification. Read the file, then:
- Remove `einstein_analysis` and `staff_architect_review` from `required` array (if present)
- Add `"pre_synthesis_path": {"type": "string", "pattern": "^/", "description": "Absolute path to pre-synthesis.md generated by inter-wave script"}` to `properties`
- Add `"pre_synthesis_path"` to the `required` array
- Also add `"description"` to `properties` per Gap 1 above

### Gap 3: D-3 acceptance criteria buried in wrong section
The acceptance criteria for wave failure propagation are in the Implementation Pathway section, not next to D-3. The criteria are:
> "After wave N completes, if any member has `status: failed`, skip subsequent waves and set their members to `status: skipped`."
Add `cmd/goyoke-team-run/wave.go` to TC-013c's Files to Create/Modify table. Add a corresponding test to `wave_test.go`.

### Gap 4: No verification step for Phase 0
After making all Phase 0 edits, run:
```bash
go test ./cmd/goyoke-team-run/... && echo "Phase 0 verified"
```
If envelope tests fail, the `description` field addition may need adjustment in test fixtures.

### Gap 5: Envelope builder fix is untested
The conservative recommendation (add `description` to schemas) has not been verified against `envelope_test.go`. The test fixtures in `envelope_test.go` add synthetic `"task": "some string"` fields. After adding `description` to schemas, generated stdin files should use `description` (not `task`) since `task` conflicts with `worker.json`'s `task` object field. Verify that `buildPromptEnvelope` accepts `description` without `task` — the code at `envelope.go:76-81` checks `stdin.Task` first, then falls back to `stdin.Description`.

---

## Executive Summary

The TC-013a/b/c decomposition is structurally sound but contains a systemic integration gap: the tickets were written against the schema layer of the system while the runtime enforces a different, incompatible contract through the envelope builder. Both analyses independently discovered this tension but framed it differently -- Einstein identified it as a four-layer schema authority problem (the deepest finding), while Staff-Architect surfaced it through the concrete `changed_files` vs `files` mismatch and budget arithmetic. The combined picture reveals that an implementer following these tickets precisely will produce stdin files that pass CI schema validation but fail at runtime, a budget ceiling that blocks the braintrust synthesis agent, and test expectations for wave failure behavior that does not exist in the binary.

Three actions must happen before implementation begins (estimated 30-60 minutes of decision-making):
1. Resolve the envelope builder's `task`/`description` requirement vs schema incompatibility (root cause of multiple findings).
2. Set the braintrust budget to $16.00 (the $5.00 default is arithmetically certain to block Wave 2).
3. Scope the `wave.go` failure propagation change as a TC-013c deliverable.

After these decisions, the tickets become implementable in sequence with high confidence.

---

## Problem Statement

### Original Request
Review the quality, specificity, and integration completeness of three decomposed implementation tickets (TC-013a: Review Workflow, TC-013b: Braintrust Workflow, TC-013c: Implementation Workflow) against the actual `goyoke-team-run` backend system.

### Clarified Problem
These tickets were produced after the first Braintrust analysis recommended decomposing monolithic TC-013 into three workflow-specific tickets. The concern is whether the new tickets are specific enough for an implementer to execute without hitting runtime surprises -- particularly around stdin schema compliance, envelope builder validation, wave execution semantics, and config generation detail.

### Success Criteria
- Every stdin file described in the tickets would be accepted by `buildPromptEnvelope()` at runtime.
- Budget arithmetic allows all agents to run in each workflow.
- Test expectations match actual binary behavior (or the binary modifications are explicitly scoped).
- Config generation has enough detail for deterministic output.

---

## Analysis Perspectives

### Einstein (Theoretical Analysis)

**Focus**: Schema authority layers, envelope builder contract analysis, data flow lifecycle, first-principles derivation of what the runtime actually requires.

**Key Insights**:
1. The system has four distinct "truth" authorities for stdin structure (formal schemas, contract templates, bridge documents, envelope builder) that are mutually incompatible.
2. Schema-compliant stdin files will be REJECTED at runtime because the envelope builder requires a top-level `task` (string) or `description` (string) that no formal schema includes.
3. Beethoven's stdin lifecycle has an architectural gap -- its schema requires Wave 1 output objects, but stdin is written before Wave 1 runs, and no mechanism exists to inject outputs into stdin between waves.

**Root Cause Identified**: Four distinct "truth" authorities for stdin structure with no reconciliation layer. The envelope builder's `task` field serves a display/routing purpose while domain schemas serve data delivery -- two concerns accidentally collapsed into one validation check.

**Novel Approaches Proposed**:
- **Modify envelope builder** (recommended): Accept any stdin with `agent` + `context` + at least one domain field, removing the hard `task`/`description` requirement. ~10 lines of Go. Eliminates findings F-01, F-10, and all test fixture hacks.
- **Adapter pattern**: Generate schema-compliant JSON, then add `task`/`description` as an envelope-compatibility step.
- **Separate generation vs execution correctness**: Schema compliance for CI, envelope compatibility for runtime -- acknowledge these are different concerns.

**Assumptions Surfaced**: 7 assumptions identified, 5 with LOW confidence.

---

### Staff-Architect (Practical Review)

**Focus**: Budget arithmetic verification, wave execution behavior validation, schema/contract compliance audit, dependency mapping, risk quantification.

**Verdict**: CONCERNS

**Issue Summary**:
- Critical: 3
- Major: 7
- Minor: 6

**Key Concerns**:
1. Budget of $5.00 is arithmetically certain to block Wave 2 (Einstein reserves $5.00, Staff-Architect never starts, Beethoven never runs).
2. TC-013c Test Case 4 expects wave failure propagation that `wave.go` does not implement, and `wave.go` is not in the ticket's Files to Create/Modify.
3. `review-architect.json` contract uses `changed_files` where the schema requires `files` -- an implementer following the contract produces schema-invalid stdin.

**Commendations (5)**:
- Excellent decomposition and sequencing (review validates pattern, braintrust validates multi-wave, implementation builds on both)
- High-quality bridge documents with corrections checklists
- Strong source-of-truth hierarchy (Go source > schemas > bridge docs > TC-013 inline)
- Feature flag for gradual rollout
- Sound budget mechanism design (the numbers are wrong but the architecture is right)

---

## Convergence Points

### Where Both Agree

| Topic | Conclusion | Confidence |
|-------|------------|------------|
| Schema/contract duality is unresolved | The `review-architect.json` contract omits the required `files` field, producing schema-invalid stdin | HIGH |
| Wave failure propagation is not implemented | `runWaves` iterates unconditionally; TC-013c tests behavior that does not exist | HIGH |
| Config generation detail is insufficient | Tickets say "generate config" without specifying `model`, `timeout_ms`, `max_retries`, stdin/stdout naming conventions | HIGH |
| `focus_areas` has multiple incompatible representations | Three different shapes across schemas, contracts, and bridge docs | HIGH |
| Feature flag (`use_team_pattern`) is undefined | `settings.json` does not exist; no schema, no default value, no code reads it | HIGH |
| Session directory creation is underspecified | Tickets say "create team directory" but do not specify path conventions, naming, or env var source | MEDIUM |
| Stdout filename mismatch in braintrust workflow | `braintrust.json` says `stdout_staff-architect.json`; `goyoke-team-prepare-synthesis` reads `stdout_staff-arch.json` | HIGH (Einstein) / Not separately flagged (Staff-Architect) |

### Complementary Insights

| Einstein Contribution | Staff-Architect Contribution | Combined Value |
|----------------------|------------------------------|----------------|
| Four-layer schema authority model (formal schemas, contracts, bridge docs, envelope builder) with structural incompatibility analysis | Concrete verification that `estimateCost("opus")` returns $5.00 (config.go line 379) and budget arithmetic proof | Einstein provides the architectural "why" (schema layers are unreconciled), Staff-Architect provides the arithmetic proof (specific dollar amounts that make it fail). Together: both the design flaw AND its quantified impact are documented. |
| Envelope builder requires `task`/`description` string that no schema includes; test fixtures work around this with synthetic fields | Severity enum mismatch between `/team-result` SKILL.md (5-level uppercase) and contracts (3-level lowercase) | Einstein found the runtime validation gap; Staff-Architect found the output parsing gap. Both are integration seams where the tickets assume consistency that does not exist. |
| Beethoven stdin lifecycle gap -- schema requires objects that do not exist when stdin is written | Budget reservation arithmetic showing Wave 2 is unreachable | Both are temporal ordering problems: Beethoven's data does not exist at write time, and the budget does not stretch to runtime. Staff-Architect's budget finding is more immediately actionable; Einstein's lifecycle finding requires a design decision. |
| Contrarian insight: formal schemas are not enforced at runtime -- the envelope builder is the real gatekeeper | Assumption register with 10 verified entries (A-1 through A-10), 8 confirmed true | Einstein challenges whether schema compliance even matters at runtime (it does not -- only envelope builder does); Staff-Architect verified the actual system behavior empirically. Together they establish what validation actually occurs vs what the tickets claim. |

---

## Divergence Resolution

### Divergence 1: Root Cause Framing

**Einstein**: The root cause is a four-layer schema authority problem. The envelope builder, formal schemas, contract templates, and bridge documents are four independent authorities that contradict each other. No single layer is designated as authoritative.

**Staff-Architect**: The root cause is a schema/contract duality -- two parallel stdin specifications exist and are inconsistent. Recommends declaring one as authoritative and fixing the other.

**Resolution**: Einstein's framing is more complete. The Staff-Architect correctly identifies the schema/contract tension (layers 1 and 2 in Einstein's model) but does not surface the envelope builder incompatibility (layer 4), which is the most severe problem. A stdin file that satisfies both the schema AND the contract will still be rejected by the envelope builder because neither includes a `task` string.

However, Staff-Architect's recommendation (declare authority, fix the contracts) is the correct first step for layers 1-2. The envelope builder issue (layer 4) requires a separate design decision.

**Unified position**: Two actions are needed:
1. Schema is authoritative over contracts. Fix `review-architect.json` to include `files[]`. (Staff-Architect's recommendation, correct.)
2. Either modify the envelope builder to not require `task`/`description`, or add a `description` field to all schemas. (Einstein's root fix, addresses layer 4.)

**Confidence**: High

---

### Divergence 2: Budget Numbers

**Einstein**: Notes that TC-013b's budget analysis is incomplete. Identifies that 3 Opus agents (not 2) need budget coverage. Suggests $12.00 may be insufficient if `estimateCost("opus")` returns $5.00 (but does not verify the actual return value).

**Staff-Architect**: Verifies empirically that `estimateCost("opus")` returns $5.00 (config.go line 379). Proves that even $12.00 is marginal ($5.00 + $5.00 = $10.00 reserved in Wave 1, leaving $2.00; after reconciliation maybe $6-8; Beethoven needs $5.00 reservation). Recommends $16.00.

**Resolution**: Staff-Architect's analysis is strictly superior here because it is grounded in verified source code. Einstein correctly identified the incompleteness but could not resolve it without the empirical data.

**Unified position**: Budget must be $16.00. This covers 3 Opus agents at $5.00 estimated with $1.00 margin. The budget is a reservation ceiling, not actual spend -- actual costs are reconciled to real usage.

**Confidence**: High (Staff-Architect verified `estimateCost` return value)

---

### Divergence 3: Envelope Builder -- Fix the Builder or Fix the Schemas?

**Einstein**: Strongly recommends modifying the envelope builder (`envelope.go`) to accept any stdin with `agent` + `context` + at least one domain-specific field, removing the `task`/`description` requirement. ~10 lines of Go. This is the root fix that eliminates F-01, F-10, and the test fixture workarounds.

**Staff-Architect**: Does not address the envelope builder `task`/`description` issue directly. Focuses on the schema/contract duality and recommends adding `files[]` to the architect contract.

**Resolution**: Einstein's recommendation is theoretically correct -- modifying the envelope builder is the root fix with lowest effort and highest future-proofing. However, it carries organizational risk: the envelope builder is a core component that all workflows depend on, and modifying it requires careful testing across all existing team configurations.

The pragmatic path is:

1. **Immediate (pre-TC-013a)**: Add a `description` string field to each formal schema as an optional field. This is a minimal schema change that satisfies the envelope builder without touching the builder itself. Remove `additionalProperties: false` from schemas or add `description` to the allowed properties.
2. **Follow-up (post-TC-013c)**: Evaluate whether the envelope builder's `task`/`description` requirement should be relaxed. By then, three workflows will be running and the team will have empirical data on whether the `description` field adds value or is just ceremony.

This resolves the immediate blocker without taking on the risk of modifying a core binary during a multi-ticket migration.

**Confidence**: Medium. Einstein's root fix is technically superior, but the incremental approach is more conservative. A reasonable person could choose either path.

**Remaining Uncertainty**: Whether relaxing the envelope builder would break any existing workflows or tests that depend on the `task` field being present.

---

### Divergence 4: Beethoven Stdin Lifecycle

**Einstein**: Identifies this as a CRITICAL architectural gap. Beethoven's schema requires `einstein_analysis` and `staff_architect_review` as JSON objects, but stdin is written before Wave 1 runs. No mechanism exists to inject Wave 1 outputs. The inter-wave script writes markdown (`pre-synthesis.md`), not a modified JSON stdin. Three resolution options proposed.

**Staff-Architect**: Does not flag this as a separate issue. The budget analysis (C-1) effectively subsumes it -- if Beethoven never runs due to budget exhaustion, the stdin lifecycle is moot.

**Resolution**: Einstein is correct that this is a real architectural gap, independent of the budget issue. Even with the budget fixed to $16.00, the question remains: how does Beethoven receive Wave 1 outputs?

The answer is implicit in the system design but not documented in the tickets:
- The inter-wave script (`goyoke-team-prepare-synthesis`) writes `pre-synthesis.md`.
- Beethoven's stdin should reference the pre-synthesis file path (not embed the full analysis objects).
- Beethoven (the LLM agent) reads the file at runtime using its Read tool.

This means Beethoven's formal schema (`beethoven.json`) needs revision: `einstein_analysis` and `staff_architect_review` should be either (a) file path strings that Beethoven reads at runtime, or (b) removed from required fields with the understanding that `pre-synthesis.md` is the actual input mechanism.

**Unified position**: Add to TC-013b: "Beethoven's stdin includes `pre_synthesis_path` (string) pointing to `pre-synthesis.md`. Beethoven reads this file at runtime. Update `beethoven.json` schema to require `pre_synthesis_path` instead of `einstein_analysis` and `staff_architect_review` objects."

**Confidence**: Medium. The file-path approach is the simplest mechanism that works with the existing system capabilities. But modifying a delivered schema (`beethoven.json`) may have ripple effects.

---

### Divergence 5: Stdout Filename Mismatch (F-02)

**Einstein**: Flags as CRITICAL. `braintrust.json` template says `stdout_staff-architect.json`; `goyoke-team-prepare-synthesis` reads `stdout_staff-arch.json`. Silent data loss -- synthesis binary has graceful degradation, so the workflow "succeeds" but Beethoven receives one-sided input.

**Staff-Architect**: Does not flag this as a separate finding. It may be implicitly covered under C-1/C-3 or may have been missed.

**Resolution**: Einstein is correct. This is a concrete, verifiable bug with a one-line fix. The fact that it causes silent degradation (not a crash) makes it more dangerous -- the workflow appears to work but produces inferior output.

**Unified position**: Fix the filename. Either change `braintrust.json` line 44 to `stdout_staff-arch.json` or change `goyoke-team-prepare-synthesis/main.go` line 29 to `stdout_staff-architect.json`. The template should be changed (not the binary) because the binary was delivered by TC-010 which is marked complete, and the template is being actively modified by TC-013b.

Add this as a concrete action item in TC-013b.

**Confidence**: High

---

### Unresolved Tensions

| Topic | Einstein View | Staff-Architect View | Why Unresolved |
|-------|--------------|---------------------|----------------|
| Whether config generation should be LLM-based or Go-binary-based | LLM generation is acceptable for Review/Braintrust (simple) but a Go binary is needed for Implementation (DAG computation) | Notes that LLM JSON generation is fragile and suggests monitoring; if >30% fail, escalate to Go binary | Empirical question. Cannot be resolved analytically. Must be tested during TC-013a implementation. |
| Severity enum alignment (5-level vs 3-level) | Not addressed | Flagged as M-3; recommends picking one scheme | A design decision that both analyses agree needs making but neither resolves. The 3-level scheme is simpler; the 5-level gives more granularity. Recommend 5-level (CRITICAL/HIGH/MEDIUM/LOW/INFO) with uppercase normalization, since this is what `/team-result` already expects. |

**Recommendation for Unresolved**: Proceed with LLM-based config generation for TC-013a. Measure schema validation pass rate. If <70%, design a Go helper binary before starting TC-013b. For severity enums, adopt 5-level uppercase as the canonical scheme and update contracts to match.

---

## Unified Recommendations

### Pre-Implementation Decisions (30-60 minutes)

These are not implementation tasks. They are decisions that must be made and recorded before any implementation begins.

| # | Decision | Recommended Answer | Effort to Decide | Impact if Deferred |
|---|----------|-------------------|-------------------|-------------------|
| D-1 | Envelope builder `task`/`description` requirement | Add `description` as optional field to all stdin schemas; remove `additionalProperties: false` or add `description` to allowed list | 15 min | Every spawn attempt fails at runtime |
| D-2 | Braintrust budget | $16.00 in `braintrust.json` | 5 min (one-line change) | Beethoven never runs |
| D-3 | Wave failure propagation scope | Add `wave.go` to TC-013c deliverables with explicit acceptance criteria | 10 min | Test Case 4 is untestable |
| D-4 | Schema vs contract authority | Schema is authoritative; fix contracts | 5 min | Implementer does not know which to follow |
| D-5 | Beethoven stdin mechanism | `pre_synthesis_path` string in stdin; Beethoven reads file at runtime | 10 min | Beethoven receives empty/degraded input |
| D-6 | Stdout filename alignment | Change `braintrust.json` to `stdout_staff-arch.json` | 2 min (one-line change) | Staff-Architect analysis silently lost |

### Primary Recommendation

**Fix the six pre-implementation decisions, then execute TC-013a/b/c in sequence.**

The tickets are well-decomposed and correctly sequenced. The issues found are integration seams -- places where the ticket specifications touch runtime code without tracing the full data path. Once the six decisions above are made and their one-line fixes applied, the tickets become implementable with high confidence.

**Rationale**: The findings from both analyses are predominantly about missing integration detail, not about structural problems with the decomposition. The ticket architecture (review validates pattern, braintrust validates multi-wave, implementation builds on both) is sound. The problems are at the seams, not in the structure.

**Theoretical Support**: Einstein's schema authority analysis shows that the incompatibilities are resolvable with targeted changes (10 lines in envelope.go, or adding `description` fields to schemas). The root cause is authority confusion, not fundamental design flaws.

**Practical Validation**: Staff-Architect's empirical verification (budget arithmetic, `estimateCost` return value, wave.go behavior) confirms that the issues are real and quantified, not theoretical. The fixes are concrete and small.

### Secondary Recommendations

1. **Fix stdout filename mismatch in `braintrust.json`**: Change `stdout_staff-architect.json` to `stdout_staff-arch.json` on line 44.
   - Priority: HIGH
   - Supports: TC-013b (braintrust synthesis quality)
   - Effort: 1 line

2. **Create `settings.json` with feature flag**: Define `{"use_team_pattern": false, "braintrust_budget_usd": 16.0}` in `.claude/settings.json`.
   - Priority: HIGH
   - Supports: All three tickets (gradual rollout mechanism)
   - Effort: 15 minutes

3. **Add `files[]` to `review-architect.json` contract**: Keep `changed_files` as supplementary; add `files` as the schema-required field.
   - Priority: HIGH
   - Supports: TC-013a (architect reviewer stdin generation)
   - Effort: 10 minutes

4. **Add unit test requirements for config generation**: Each ticket should specify a unit test that generates a stdin file and validates it against the corresponding formal schema.
   - Priority: MEDIUM
   - Supports: Catching schema/runtime mismatches before e2e testing
   - Effort: 30 minutes per ticket

5. **Use section headers instead of line numbers** in TC-013b's Mozart template references.
   - Priority: LOW
   - Supports: Reference stability across edits
   - Effort: 5 minutes

6. **Align severity enums**: Adopt 5-level uppercase (CRITICAL/HIGH/MEDIUM/LOW/INFO) as canonical. Update contract stdout schemas to match.
   - Priority: MEDIUM
   - Supports: TC-013a `/team-result` integration
   - Effort: 20 minutes

7. **Use `GOYOKE_SESSION_DIR`** (set by TUI) instead of `GOYOKE_SESSION_ID` across all tickets.
   - Priority: LOW
   - Supports: Correct session directory resolution
   - Effort: 10 minutes

### Not Recommended

1. **Modifying envelope builder before TC-013a**: While Einstein's root fix (relaxing `task`/`description` requirement) is technically superior, it touches a core component during a multi-ticket migration. Adding `description` to schemas is safer and can be revisited post-migration.

2. **Building a Go helper binary for Review/Braintrust config generation**: LLM-based config generation is sufficient for these simpler workflows. Save the Go binary approach for Implementation (TC-013c) where DAG computation justifies it.

3. **Implementing all three tickets simultaneously**: Sequential execution with validation at each step is the right approach. Both analyses confirm the sequencing is sound.

---

## Implementation Pathway

### Phase 0: Pre-Implementation Decisions and Fixes (1 hour)

- [ ] D-1: Add `description` (string, optional) to `reviewer.json`, `einstein.json`, `beethoven.json`, `worker.json` schemas. Adjust `additionalProperties` accordingly.
- [ ] D-2: Change `budget_max_usd` from `5.00` to `16.00` in `schemas/teams/braintrust.json`.
- [ ] D-3: Add `cmd/goyoke-team-run/wave.go` to TC-013c Files to Create/Modify. Write acceptance criteria for failure propagation.
- [ ] D-4: Add `files[]` to `review-architect.json` contract alongside `changed_files`.
- [ ] D-5: Update `beethoven.json` schema: replace `einstein_analysis`/`staff_architect_review` objects with `pre_synthesis_path` string. Update TC-013b to document this mechanism.
- [ ] D-6: Change `stdout_staff-architect.json` to `stdout_staff-arch.json` in `braintrust.json` template.
- [ ] Create `.claude/settings.json` with `{"use_team_pattern": false}`.
- [ ] Run `go test ./cmd/goyoke-team-run/...` to verify no regressions from schema/template changes.

### Phase 1: TC-013a -- Review Workflow (1-2 days)

- [ ] Implement `/review` skill's team dispatch path (feature flag gated)
- [ ] Router generates config.json + reviewer stdin files (with `description` field)
- [ ] Validate generated stdin against `schemas/stdin/reviewer.json`
- [ ] Launch `goyoke-team-run` and verify background PID
- [ ] Test: review workflow produces valid stdout consumed by `/team-result`
- [ ] Verify severity enum alignment between stdout and `/team-result` parsing

### Phase 2: TC-013b -- Braintrust Workflow (2-3 days)

- [ ] Reconcile Mozart Phase 2.5 stdin templates with corrected schemas
- [ ] Implement braintrust config generation with $16.00 budget
- [ ] Verify inter-wave script produces `pre-synthesis.md`
- [ ] Verify Beethoven receives and reads `pre_synthesis_path`
- [ ] Test: full braintrust run produces analysis document
- [ ] Verify stdout filename consistency (`stdout_staff-arch.json`)

### Phase 3: TC-013c -- Implementation Workflow (2-3 days)

- [ ] Implement wave failure propagation in `wave.go` (per D-3 acceptance criteria)
- [ ] Build `goyoke-plan-impl` binary with specs.md parsing and Kahn's algorithm
- [ ] Test: multi-wave execution with deliberate Wave 1 failure
- [ ] Test: various DAG structures (1 wave, 2 waves, circular dependency detection)

### Decision Points

1. **After Phase 1**: Measure LLM config generation reliability. If schema validation pass rate is <70%, design a Go helper binary before Phase 2.
2. **After Phase 2**: Evaluate whether the `description` field in schemas is adding value or is just ceremony. If ceremony, consider Einstein's envelope builder modification as a follow-up.
3. **Before Phase 3**: Confirm that wave failure propagation semantics are correct: "After wave N completes, if any member has `status: failed`, skip subsequent waves and set their members to `status: skipped`."

---

## Risk Assessment

### Identified Risks

| Risk | Source | Likelihood | Impact | Mitigation |
|------|--------|------------|--------|------------|
| Schema-compliant stdin rejected by envelope builder | Einstein (F-01) | Certain (without D-1 fix) | Critical (every spawn fails) | D-1: Add `description` to schemas |
| Budget blocks Beethoven in braintrust workflow | Staff-Architect (C-1) | Certain (without D-2 fix) | Critical (no synthesis) | D-2: Raise budget to $16.00 |
| Staff-Architect stdout silently lost in synthesis | Einstein (F-02) | Certain (without D-6 fix) | High (one-sided analysis) | D-6: Fix filename in template |
| Beethoven receives empty/degraded input | Einstein (F-03) | High (without D-5 fix) | High (synthesis quality) | D-5: Use `pre_synthesis_path` mechanism |
| Wave failure propagation test is untestable | Both (F-04, C-2) | Certain (without D-3 fix) | Medium (test failure, not runtime) | D-3: Scope `wave.go` change |
| LLM generates invalid JSON for config | Staff-Architect (M-1) | Medium | High (launch fails) | Unit test validation; escalate to Go binary if >30% fail |
| Reviewer agents produce unexpected stdout format | Staff-Architect (A-10) | Medium | Medium (team-result parsing fails) | `validateStdout` exists in spawn.go; handle malformed output gracefully |
| `goyoke-team-prepare-synthesis` not on PATH | Staff-Architect (M-7) | Low | High (Wave 2 never starts) | Document in prerequisites; consider absolute path in template |
| `settings.json` does not exist | Both (M-2, F-11) | Certain (without fix) | Low (LLM uses default) | Create file in Phase 0 |

### Assumptions to Validate

| Assumption | How to Validate | Before Phase |
|------------|-----------------|--------------|
| Adding `description` to schemas does not break existing tests | Run `go test ./cmd/goyoke-team-run/...` after schema modification | Phase 0 |
| LLM can reliably generate schema-compliant JSON configs | Measure pass rate during TC-013a implementation | Phase 1 |
| `goyoke-team-prepare-synthesis` handles missing stdout gracefully | Run binary with one missing stdout file, verify degraded-but-functional output | Phase 2 |
| Beethoven can read `pre-synthesis.md` via Read tool from a path in stdin | Test with a sample braintrust run | Phase 2 |
| Wave failure propagation does not break existing review workflow (which has no inter-wave dependencies) | Run review workflow after wave.go modification | Phase 3 |

---

## Open Questions

Questions requiring human decision or empirical data:

1. **Should the envelope builder be relaxed?** Einstein's root fix (~10 lines in `envelope.go`, removing `task`/`description` requirement) is technically superior to adding `description` fields to all schemas. The incremental approach (add `description`) is recommended here for safety, but the envelope builder change should be evaluated as a follow-up. **Decision needed**: After TC-013c completes, should a follow-up ticket relax the envelope builder?

2. **What `description` text should each workflow generate?** With `description` added as a schema field, implementers need to know what string to put there. Suggested defaults:
   - Reviewer: `"Review {domain} code changes for {focus_areas}"`
   - Einstein: `"Theoretical analysis: {problem_brief.title}"`
   - Beethoven: `"Synthesize analyses for: {problem_brief.title}"`
   - Worker: `"Implement: {task.subject}"`

3. **Should `focus_areas` be standardized?** Three incompatible representations exist. For immediate purposes, the schema's generic `type: object` is fine (LLMs handle any shape). But should a canonical structure be defined for future CI validation? **Recommended**: Defer. The LLM flexibility is an asset here. Standardize only if validation problems emerge.

4. **Is LLM-based config generation reliable enough?** Staff-Architect recommends monitoring and escalating to a Go binary if >30% of configs fail validation. What is the acceptable failure rate, and who monitors it? **Recommended**: Accept LLM generation for TC-013a; measure during implementation; make the Go binary decision before TC-013b.

5. **Who owns the `review-architect.json` contract fix?** The contract was delivered by TC-009. Adding `files[]` to it is a retroactive fix to a completed ticket. Should this be a sub-task of TC-013a or a separate micro-ticket? **Recommended**: Sub-task of Phase 0, since it is a 10-minute fix.

---

## Appendix: Full Analyses

<details>
<summary>Einstein Theoretical Analysis (Full)</summary>

# Einstein Theoretical Analysis: TC-013a/b/c Ticket Quality Review

> **Problem Brief**: TC-013a/b/c ticket quality and integration completeness
> **Analysis Focus**: Schema duality, envelope builder mismatches, wave failure propagation, config generation specificity, cross-ticket coherence
> **Timestamp**: 2026-02-08T15:30:00Z

## Executive Summary

The three TC-013 decomposition tickets (a/b/c) represent a significant improvement over the monolithic TC-013, but they contain a **fundamental unaddressed design tension**: the system has three separate schema authorities (formal JSON schemas, contract templates, and bridge document examples) that contradict each other, and the envelope builder validates against a fourth implicit contract (top-level `task` string) that none of the domain schemas satisfy. An implementer following these tickets will hit a runtime failure on the first spawn attempt because every domain-specific stdin file will fail envelope validation. Additionally, the wave execution engine does not propagate member failures between waves, directly contradicting TC-013c's test expectations.

## Root Cause Analysis

### Surface Problem
The tickets "lack specificity and detail for integration with the actual backend system."

### Underlying Cause
The tickets were written against a mental model of the system rather than the actual code. They reference schemas and binaries but do not trace the full data flow from config generation through envelope building to CLI invocation and back through stdout processing.

### Fundamental Issue
**The system has an impedance mismatch between its validation layers.** There are four distinct "truth" authorities for what a stdin file should look like:

1. **Formal JSON Schema** (`schemas/stdin/reviewer.json`, `einstein.json`, `worker.json`) -- defines required fields, types, and `additionalProperties: false`
2. **Contract Templates** (`schemas/teams/stdin-stdout/review-backend.json`, etc.) -- shows "example" data shapes that sometimes contradict the formal schema
3. **Bridge Document Examples** (`ReviewOrch-team-bridge.md` Section 3) -- provides "validated" examples that use yet a third variant of some fields
4. **Envelope Builder** (`envelope.go`) -- the actual runtime validation, which only checks for top-level `agent` (string), `context` (non-empty object), and `task` OR `description` (non-empty string)

These four layers are not reconciled. Any stdin file that satisfies one layer may violate another. The tickets instruct implementers to comply with layer 1 (formal schemas) but the runtime enforces layer 4 (envelope builder), and these two are structurally incompatible.

### Evidence Chain

The envelope builder (envelope.go line 16-24) defines:
```go
type StdinEnvelope struct {
    Agent       string                 `json:"agent"`
    Context     map[string]interface{} `json:"context"`
    Task        string                 `json:"task,omitempty"`
    Description string                 `json:"description,omitempty"`
    raw json.RawMessage
}
```

- **Reviewer stdin** (per `reviewer.json` schema): Has NO `task` or `description` field. Has `review_scope`, `git_context`, `focus_areas`. Envelope builder will reject with "task field is empty."
- **Einstein stdin** (per `einstein.json` schema): Has NO `task` or `description` field. Has `problem_brief`, `codebase_context`. Envelope builder will reject with "task field is empty."
- **Worker stdin** (per `worker.json` schema): Has `task` as an OBJECT (with `task_id`, `subject`, etc.), not a string. Go's `json.Unmarshal` into `string` silently produces empty string. Envelope builder will reject.

The envelope tests (envelope_test.go) work around this by adding a synthetic `"task": "some string"` field to every test stdin, but this field is NOT in any of the formal schemas (which have `additionalProperties: false`). The tests pass because they test the envelope builder against its own expectations, not against actual schema-compliant stdin files.

## Conceptual Framework

### Primary Lens: Schema Evolution and Authority Confusion

In any system with multiple schema layers, one of two conditions must hold:

**Condition A (Layered Validation):** Each layer validates a subset. Layer N passes through fields it does not understand to layer N+1. Validation is compositional -- satisfying all layers is achievable by satisfying each independently.

**Condition B (Authoritative Schema):** One layer is designated as authoritative. All other representations are derived from it. Contradictions are resolved by reference to the authority.

The goYoke system satisfies neither condition:
- The envelope builder validates `task` (string), but formal schemas define `task` (object) or omit it entirely. These are not composable.
- No single layer is designated as authoritative in the tickets. TC-013a says "must comply with `schemas/stdin/reviewer.json`" but the envelope builder will reject schema-compliant files.

**Key Insight:** The envelope builder's `task` field serves as a **display/routing purpose** (used in the prompt envelope for the `AGENT:` header context), while the formal schema's structured fields serve as **domain-specific data delivery**. These are two different concerns that have been accidentally collapsed into a single validation check.

### Alternative Lens: Contract vs. Specification

The dual schema system (formal schemas + contract templates) can be understood as:
- **Specifications** (formal schemas): What a stdin file MUST contain for correctness
- **Contracts** (templates): What a stdin file SHOULD look like for a specific reviewer type

These should be in a subtype relationship: every contract instance should validate against the specification. Currently they do not. The `review-architect.json` contract uses `changed_files` where the spec requires `files`. The backend contract uses nested `{enabled, priorities}` objects for `focus_areas` while the bridge document uses simple booleans.

**Key Insight:** The contract templates appear to have been designed independently of the formal schemas, probably by different agents or at different times. They serve as useful documentation of intent but have not been validated against the specifications they purport to instantiate.

## First Principles Analysis

### Starting Axioms

1. **The Go binary is the execution authority.** Whatever `buildPromptEnvelope()` accepts is what actually runs. Everything else is documentation.
2. **Schema-compliant stdin files must actually work.** If a ticket says "generate stdin compliant with X schema" and that stdin is rejected by the binary, the ticket is wrong.
3. **Wave execution semantics determine what is possible.** If `runWaves` does not check for member failures between waves, then test cases that expect failure propagation are testing behavior that does not exist.
4. **Config generation is deterministic input to a deterministic binary.** The config.json + stdin files fully determine execution. Any ambiguity in how to generate them is an ambiguity in the ticket.

### Derived Implications

- From Axioms 1+2: **Every ticket must include a "task" or "description" string in its stdin specification**, even though the formal schemas do not require it. This is an envelope builder requirement that supersedes schema requirements. The tickets must either (a) add this field to all schemas, or (b) modify the envelope builder to not require it, or (c) document this requirement explicitly.
- From Axiom 3: **TC-013c Test Case 4 ("Task failure mid-wave") is testing unimplemented behavior.** The ticket says "Wave 2 tasks have status implications" but `runWaves` proceeds unconditionally. Either the test expectation must be removed, or `runWaves` must be modified, and the ticket must specify which.
- From Axiom 4: **Tickets that say "generate config.json from template" without specifying which fields to populate, with what values, and from what data source are under-specified.**

### Novel Conclusions

The system actually needs a **fifth layer**: a "runtime-compatible schema" that is the intersection of the formal schema and the envelope builder's requirements. This schema would include the domain-specific required fields AND the envelope builder's `task`/`description` string requirement. Currently this intersection does not exist as an artifact -- it exists only implicitly in the test fixtures.

## Finding Inventory

### F-01: Envelope Builder / Schema Incompatibility [CRITICAL]
Affected tickets: TC-013a, TC-013b, TC-013c (all three). The envelope builder requires a top-level `task` (string) or `description` (string) field. None of the three formal schemas include this field. Schemas with `additionalProperties: false` will reject it if added. Schema-compliant stdin files will be rejected by the envelope builder at runtime.

### F-02: Stdout Filename Mismatch in Inter-Wave Script [CRITICAL]
Affected ticket: TC-013b. The `braintrust.json` template names staff-architect stdout `stdout_staff-architect.json` but `goyoke-team-prepare-synthesis` reads `stdout_staff-arch.json`. Silent data loss.

### F-03: Beethoven Stdin Lifecycle Gap [CRITICAL]
Affected ticket: TC-013b. Beethoven's schema requires `einstein_analysis` and `staff_architect_review` as JSON objects, but stdin is written before Wave 1 runs. No mechanism to inject Wave 1 outputs into Wave 2 stdin.

### F-04: Wave Failure Propagation Not Implemented [HIGH]
Affected ticket: TC-013c. `runWaves` iterates unconditionally. TC-013c Test Case 4 expects failure propagation.

### F-05: Review Schema Duality -- `changed_files` vs `files` [HIGH]
Affected ticket: TC-013a. Architect contract uses `changed_files`; schema requires `files`.

### F-06: Three Incompatible `focus_areas` Representations [HIGH]
Affected ticket: TC-013a. Schema says generic object, contracts say nested `{enabled, priorities}`, bridge doc says simple booleans.

### F-07: `project_conventions` Structure Varies Per Reviewer [MEDIUM]
Affected ticket: TC-013a. Different structures across reviewer contracts.

### F-08: Missing Config Generation Detail [HIGH]
Affected tickets: TC-013a, TC-013b, TC-013c. `model`, `timeout_ms`, `max_retries`, stdin/stdout naming not specified.

### F-09: Session Directory Creation Not Specified [MEDIUM]
Affected tickets: TC-013a, TC-013b, TC-013c. Path conventions, naming, env var source not documented.

### F-10: No Specification for `description` Field to Satisfy Envelope Builder [HIGH]
Affected tickets: TC-013a, TC-013b. What string to put in `task`/`description` is unspecified.

### F-11: Cross-Ticket Coherence -- Feature Flag [LOW]
Affected tickets: All. `settings.json` does not exist.

### F-12: TC-013b Budget Analysis Incomplete [MEDIUM]
Affected ticket: TC-013b. Budget calculation does not account for 3 Opus agents.

## Novel Perspectives

### Inversion: What if the Envelope Builder is Wrong?
The envelope builder's `task` string requirement was designed for a simpler era. Rather than bending all schemas to satisfy it, the builder should validate the minimum viable contract: `agent` (non-empty) + `context` (non-empty) + at least one domain-specific field. ~10-line change that eliminates F-01, F-10, and test fixture hacks.

### The Adapter Pattern
A "stdin adapter" step could be added to config generation: generate schema-compliant JSON, then add the `task`/`description` string required by the envelope builder.

### Contrarian View
The formal schemas are not enforced at runtime. The envelope builder is the real gatekeeper. The tickets' emphasis on schema compliance is aspirational, not functional.

## Severity Summary

| Severity | Count | Findings |
|----------|-------|----------|
| CRITICAL | 3 | F-01, F-02, F-03 |
| HIGH | 5 | F-04, F-05, F-06, F-08, F-10 |
| MEDIUM | 3 | F-07, F-09, F-12 |
| LOW | 1 | F-11 |

</details>

<details>
<summary>Staff-Architect Practical Review (Full)</summary>

# Critical Review: TC-013a/b/c Team-Run Migration Tickets

**Reviewed:** 2026-02-08T16:45:00Z
**Reviewer:** Staff Architect Critical Review
**Verified Against:** Go source (`cmd/goyoke-team-run/*.go`), JSON schemas (`schemas/stdin/*.json`), team templates (`schemas/teams/*.json`), contract files (`schemas/teams/stdin-stdout/*.json`), review skill (`skills/review/SKILL.md`), team-result skill (`skills/team-result/SKILL.md`)

## Executive Assessment

**Overall Verdict:** CONCERNS
**Confidence Level:** HIGH

**Issue Counts:**
- Critical: 3 (must fix)
- Major: 7 (should fix)
- Minor: 6 (consider fixing)
**Commendations:** 5

**Go/No-Go Recommendation:**
Do NOT start implementation until C-1 (budget decision), C-2 (wave failure propagation scope), and C-3 (schema authority resolution) are resolved. These are 30-minute decisions, not days of work.

## Critical Issues

### C-1: Braintrust budget arithmetic blocks Wave 2
Budget = $5.00. `estimateCost("opus")` returns $5.00 (verified: config.go line 379). Einstein reserves $5.00 -> remaining $0.00. Staff-Architect budget-gated. Beethoven never runs.
**Recommendation:** Raise to $16.00.

### C-2: Wave failure propagation requires unscoped code change
TC-013c Test 4 expects behavior `wave.go` does not implement. `wave.go` not listed in Files to Create/Modify.
**Recommendation:** Add `wave.go` to deliverables with explicit acceptance criteria.

### C-3: Schema vs contract duality unresolved
`review-architect.json` uses `changed_files`; schema requires `files`. Implementer does not know which to follow.
**Recommendation:** Schema is authoritative; fix architect contract.

## Major Issues

- M-1: No code/pseudocode for config generation
- M-2: `settings.json` with `use_team_pattern` does not exist
- M-3: Severity enum mismatch (5-level uppercase vs 3-level lowercase)
- M-4: No unit tests for config generators
- M-5: Review SKILL.md rewrite scope undersold
- M-6: Mozart template referenced by fragile line numbers
- M-7: Inter-wave script PATH verification unscoped

## Minor Issues

- m-1: Session ID source unclear (`GOYOKE_SESSION_ID` vs `GOYOKE_SESSION_DIR`)
- m-2: specs.md format not formally specified
- m-3: Kahn's algorithm reference (minor documentation)
- m-4: Feature flag checked by LLM, not programmatically
- m-5: Einstein-only detection logic unspecified
- m-6: Agent validation recommended but not required

## Assumption Register (10 entries, 8 verified, 2 unverified)

Key unverified assumptions:
- A-7: `settings.json` exists and is readable (NOT verified -- file does not exist)
- A-10: Reviewer agents produce JSON stdout matching contract schema (NOT verified -- LLMs may produce arbitrary output)

## Commendations (5)

1. Excellent decomposition and sequencing
2. High-quality bridge documents
3. Strong source-of-truth hierarchy
4. Feature flag for gradual rollout
5. Sound budget mechanism design

## Recommended Implementation Order

1. Resolve C-1, C-2, C-3 decisions (30 minutes)
2. Create `settings.json` (15 minutes)
3. Fix `review-architect.json` contract (15 minutes)
4. TC-013a implementation
5. TC-013b (with updated budget)
6. TC-013c (with wave.go modification)

## Risk Heat Map

| Phase | Risk | Likelihood | Impact |
|-------|------|------------|--------|
| TC-013b | Budget blocks Beethoven | **Certain** | **Critical** |
| TC-013c | Wave 2 runs despite Wave 1 failure | **Certain** | Medium |
| All | `settings.json` does not exist | **Certain** | Low |
| TC-013a | LLM generates invalid JSON | Medium | High |
| TC-013a | Reviewer agents produce unexpected stdout | Medium | Medium |

</details>

---

## Metadata

```yaml
braintrust_analysis_id: tc013abc-ticket-quality-2026-02-08
problem_brief_id: tc013abc-quality-review
timestamp: 2026-02-08T17:30:00Z
session_id: braintrust-tc013abc-quality
prior_analysis: analysis-tc013-alignment-review.md

agents_invoked:
  - mozart
  - einstein
  - staff-architect-critical-review
  - beethoven

convergence_points: 7
divergence_points: 5
contradictions_resolved: 5
contradictions_unresolved: 2

recommendations_count: 13 (6 pre-impl decisions + 7 secondary)
risks_identified: 9
open_questions: 5
assumptions_to_validate: 5

einstein_findings: 12 (3 critical, 5 high, 3 medium, 1 low)
staff_architect_findings: 16 (3 critical, 7 major, 6 minor)
staff_architect_commendations: 5

source_analyses:
  - /home/doktersmol/Documents/goYoke/.claude/braintrust/einstein-tc013-review.md
  - /home/doktersmol/Documents/goYoke/.claude/braintrust/staff-architect-tc013-review.md
```
