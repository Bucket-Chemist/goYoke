# Problem Brief

> **Generated by Mozart**
> **Timestamp:** 2026-02-05T14:30:00Z
> **Session:** braintrust-tui-rendering

---

## 1. Problem Statement

### Original Input
TUI rendering bugs observed via screenshot analysis:
1. Session List Overflow - 12+ identical session entries stacking at top
2. ASCII Banner Corruption - Garbled/overlapping text on resize
3. Text Overprinting - New content prints over existing in conversation
4. Layout Proportions - Session list dominates vs. conversation area
5. Wasted Right Panel - "No agents yet" panel takes 30% with little value

### Clarified Statement (Post-Interview)
The user has confirmed:
- **Success criteria**: Both bug fixes AND redesign recommendations needed
- **Session list behavior**: The 12+ entries are a BUG - should show only ONE current session
- **Constraints**: Open to restructuring component hierarchy if it improves UX

**Critical clarification from code review**: There is NO `SessionList.tsx` component. What appears as "session list" in the screenshot is actually the **AgentTree** component (`/home/doktersmol/Documents/goYoke/packages/tui/src/components/AgentTree.tsx`). The 12+ identical entries are likely agents being added repeatedly to the store, not sessions.

### Success Criteria
1. **Bug diagnosis**: Root cause analysis for each rendering issue
2. **Fix recommendations**: Concrete code changes with file/line references
3. **Redesign proposals**: Alternative component layouts to improve UX
4. **Implementation priority**: Ordered by impact vs. effort

---

## 2. Scope Assessment

### Files in Scope

| File | Relevance | Lines | Purpose |
|------|-----------|-------|---------|
| `src/components/Layout.tsx` | **Primary** | 118 | Main 70/30 split, height calculations, panel composition |
| `src/components/ClaudePanel.tsx` | **Primary** | 372 | Message viewport, streaming, text rendering |
| `src/components/Banner.tsx` | **Secondary** | 55 | Fixed 3-row banner, model/cost display |
| `src/components/AgentTree.tsx` | **Primary** | 168 | Hierarchical agent tree (the "session list" issue) |
| `src/components/AgentDetail.tsx` | **Secondary** | ~50 | Agent detail panel (40% of right panel) |
| `src/components/primitives/Viewport.tsx` | **Primary** | 136 | Scrollable list, auto-scroll, resize handling |
| `src/store/slices/agents.ts` | **Secondary** | 83 | Agent state management, addAgent logic |
| `src/store/index.ts` | **Secondary** | 63 | Zustand store with persistence |
| `src/App.tsx` | **Context** | 164 | Entry point, session loading |
| `src/config/theme.ts` | **Context** | ~50 | Colors, borders, icons |

### Total Scope
- **10 files**, ~1,200 lines
- **Framework**: React 18 + Ink 5 (terminal UI)
- **State**: Zustand with persistence middleware
- **Key constraint**: Ink's flexbox-in-terminal model differs from browser DOM

### Complexity Signals
1. **Terminal rendering quirks**: Ink uses Yoga (flexbox) but with terminal cell constraints
2. **Height calculation cascade**: `Layout.tsx` line 34-35 calculates from `stdout.rows`
3. **State persistence**: `store/index.ts` persists agents - stale state may cause duplicates
4. **No explicit resize handler**: Components rely on React re-render on stdout change
5. **Viewport scroll state**: `Viewport.tsx` manages its own scroll offset, may not sync with parent height changes

### Prior Art / Existing Patterns
- `ResponsiveLayout.tsx` and `LayoutSpike.tsx` exist as test/spike components
- `BorderStyleTest.tsx` suggests prior debugging of border rendering issues
- Store has `clearAgents()` method but unclear when it's called

---

## 3. Analysis Axes

### For Einstein (Theoretical)

**Primary question**: What are the fundamental rendering model mismatches between Ink's terminal flexbox and the expected visual behavior?

**Conceptual focus**:
- Terminal cell grid vs. flexbox percentage calculations
- React reconciliation with terminal cursor positioning
- State persistence causing ghost data on reload
- Height constraint propagation through component tree

**Novel angles**:
- Is the 12-agent duplication a state hydration bug from Zustand persist?
- Are percentage widths ("70%", "30%") causing fractional cell rounding issues?
- Does Ink's `overflow="hidden"` actually prevent content from rendering, or just from being visible?

### For Staff-Architect (Practical)

**Review focus**:
- Layout.tsx height calculation correctness (lines 14-15, 34-35, 72-73)
- AgentTree.tsx recursive rendering without memoization
- Viewport.tsx scroll state vs. parent height sync
- Store persistence config potentially hydrating stale agents

**Risk areas**:
- `height="100%"` on ClaudePanel (line 319) may conflict with explicit `maxHeight` prop
- `flexGrow={1}` usage in Viewport may cause expansion beyond container
- No `key` stability check on agent rendering could cause React reconciliation issues

**Constraint check**:
- Must maintain 70/30 split ratio
- Banner must stay fixed at top (3 rows)
- Modal overlay rendering must remain last in DOM order

---

## 4. Constraints

1. **Framework**: Cannot switch from Ink - it's the chosen terminal UI framework
2. **React version**: Must stay React 18 compatible
3. **State management**: Zustand is established, avoid migration
4. **Performance**: Agent tree must handle 20+ agents without lag
5. **Backward compatibility**: Store schema changes must not break existing persisted sessions

---

## 5. Anti-Scope

Analysis should NOT:
- Propose switching to a different TUI framework (blessed, terminal-kit, etc.)
- Redesign the overall application architecture beyond the rendering layer
- Address non-rendering bugs (API errors, auth, etc.)
- Optimize for extreme scale (1000+ agents) - 50 is sufficient target
- Add new features - focus on fixing existing rendering

---

## 6. Key Questions for Analysts

1. **Why are 12+ identical agent entries appearing?** Is this:
   - Store hydration from persistence loading stale data?
   - Multiple `addAgent` calls without deduplication?
   - React key instability causing re-renders that look like duplicates?

2. **Why does text overprint on resize?** Is this:
   - Ink not clearing previous render before new render?
   - Height calculation changing mid-render?
   - Viewport scroll position becoming invalid after resize?

3. **Why is the banner corrupted?** Is this:
   - Unicode characters not rendering in terminal width calculation?
   - Border style characters being double-width in some terminals?
   - Text wrapping when it shouldn't?

4. **Should the right panel be collapsible/hidden when empty?** UX consideration.

---

## Metadata

```yaml
problem_brief_id: pb-tui-rendering-2026-02-05
interview_questions_asked: 3
scouts_spawned: 0
files_in_scope: 10
estimated_lines: 1200
estimated_analysis_tokens: ~25000
```
