# Braintrust Analysis: TC-013 Alignment Review

> **Generated by Beethoven**
> **Timestamp**: 2026-02-08T12:00:00Z
> **Problem Brief**: Is TC-013 up-to-date and aligned with actual implementations?
> **Input Analyses**:
>   - Einstein Theoretical Analysis (root cause decomposition, novel approaches)
>   - Staff-Architect Critical Review (binary bugs, schema drift, implementation order)

---

## Executive Summary

**TC-013 is not up to date.** It is structurally misaligned with the implementations delivered by TC-008, TC-009, TC-010, TC-012, TC-018, and TC-019. The misalignment is not cosmetic -- it spans field naming, stdin schema structure, launch command interface, binary integration points, and budget enforcement logic. Additionally, three bugs in the Go binary (`runInterWaveScript` stub, budget double-reservation, `background_pid` never written to config.json) would cause the braintrust workflow to silently produce empty synthesis inputs, fail on budget gates before any agent starts, and report every launch as failed.

Both analyses converge on the same core conclusion: TC-013 must be rewritten from ground truth (Go source code + TC-009 schemas), not patched from its current stale inline examples. They further agree that TC-013 is actually three tickets of different complexity, and that the 4 proposed helper binaries should be consolidated to 1 or 0.

The recommended path forward: fix the three binary bugs first (0.5-1 day), complete TC-020 bridge docs (1-2 days), then decompose TC-013 into three workflow-specific tickets implemented in order of ascending complexity (Review, Braintrust, Implementation). Revised total effort: 5-8 days, up from the original 3-5 day estimate.

---

## The Core Question: Is TC-013 Aligned?

**Answer: No.** The ticket suffers from what Einstein identifies as "information decay in speculative specifications" -- it was written before the implementations it depends on existed, and each upstream ticket diverged from TC-013's assumptions. The drift is structural, not editorial.

### Evidence of Misalignment

| Category | TC-013 Assumes | Reality (Implemented) | Severity |
|----------|---------------|----------------------|----------|
| **Field names** | `agent_id`, `budget_total_usd`, `pid` | `agent`, `budget_max_usd`, `process_pid` | HIGH -- config generation will produce invalid JSON |
| **Member nesting** | Flat `members[].wave` field | Nested `waves[].members[]` | HIGH -- config structure is fundamentally different |
| **Stdin schema** | `$schema`, `agent_id`, `role`, `task`, `context.problem_brief` (path string), `reads_from`, `output_requirements` | `agent`, `workflow`, `context`, `problem_brief` (nested object with `title`, `statement`, `scope`, `complexity_signals`, `constraints`), `codebase_context` | CRITICAL -- entirely different required fields and nesting |
| **Schema paths** | `schemas/stdin/braintrust-analyst-v1.json` | `schemas/stdin/einstein.json` | MEDIUM -- wrong filenames, no `-v1` suffix |
| **Schema base URL** | `https://gogent.dev/schemas/` | `https://gogent-fortress/schemas/` | LOW -- cosmetic |
| **Launch command** | `gogent-team-run "$team_dir/config.json" > "$team_dir/launch.log" 2>&1` | `gogent-team-run "$team_dir"` (positional dir arg, binary handles own logging/daemonization) | HIGH -- wrong CLI interface |
| **Launch verification** | Reads `background_pid` from config.json | Field is never written by the binary (bug C-3) | CRITICAL -- every launch appears failed |
| **Inter-wave script** | Not referenced | `gogent-team-prepare-synthesis` exists (TC-010) but `runInterWaveScript` in wave.go is a stub | CRITICAL -- braintrust Wave 2 receives no synthesis input |
| **Helper binaries** | 4 new binaries (`gogent-team-init-review`, `gogent-team-init-impl`, `gogent-parse-specs`, `gogent-compute-waves`) | None exist; Einstein argues most are unnecessary | MAJOR -- over-engineering concern |
| **Execution paths** | Background-only via `gogent-team-run` | TC-019 confirms MCP path remains viable; two paths must coexist | MAJOR -- missing fallback architecture |

---

## Analysis Perspectives

### Einstein (Theoretical Analysis)

**Focus**: Root cause decomposition of TC-013's drift; category analysis of what TC-013 actually is; novel approaches for decomposition and simplification.

**Key Insights**:
1. TC-013's drift is structural, not cosmetic -- information debt accumulates proportionally to each upstream ticket's divergences from the original spec.
2. TC-013 conflates three distinct concerns (architectural transition design, three separate workflow rewrites, specification for 4 new Go binaries) that have different dependency and risk profiles.
3. The "orchestrator" abstraction dissolves in the background path -- when `gogent-team-run` is the orchestrator, what remains is config generation, a fundamentally different concern than LLM orchestration.
4. Mozart's Phase 2.5 stdin templates (added by TC-018) introduce a second drift layer -- they don't comply with TC-009 schemas either, creating a drift-on-drift situation.

**Root Cause Identified**: Information decay in speculative specifications. TC-013 was written as a forward-looking spec before implementations existed, and each upstream ticket's implementation decisions diverged from TC-013's assumptions without backporting corrections.

**Novel Approaches Proposed**:
- Decompose TC-013 into TC-013a (Review, simplest), TC-013b (Braintrust, moderate), TC-013c (Implementation, complex)
- Replace 4 helper binaries with 1 `gogent-team-init` with subcommands, or 0 for Review/Braintrust (LLM generates config + validation pass)
- Hybrid approach: LLM-generated config for Review, Go-assisted for Implementation, hybrid for Braintrust

**Assumptions Surfaced**: 8 assumptions identified, including that the 4 helper binaries add value (challenged) and that background is the only execution path (false per TC-019).

---

### Staff-Architect (Practical Review)

**Focus**: Binary-level bug verification; schema compliance audit; implementation ordering; effort recalibration.

**Verdict**: CONCERNS (3 Critical, 7 Major, 5 Minor)

**Issue Summary**:
- Critical: 3 (binary bugs that must be fixed before TC-013 can succeed)
- Major: 7 (structural mismatches, missing prerequisites, over-engineering)
- Minor: 5 (naming, paths, cosmetic)

**Key Concerns**:
1. **C-1: `runInterWaveScript` is a stub** (wave.go:96-102). TC-010 delivered the binary but wave.go still has a no-op. Beethoven receives empty inputs.
2. **C-2: Budget double-reservation** (wave.go:34-35 reserves, then spawn.go:63-66 reserves again). For a $6 braintrust budget with 2 Opus agents ($5 each), wave reserves $5 (remaining: $1), then Spawn tries to reserve $5 again and fails. No agent ever starts.
3. **C-3: `background_pid` never written** to config.json. The binary writes a PID *file* (daemon.go:62) but never sets `config.BackgroundPID`. TC-013's launch verification reads this field from config.json and treats null as launch failure.

**Commendations**:
- TC-013's pre-implementation notes (added 2026-02-07) already document many schema naming issues
- The reconciliation checklist at lines 129-146 is thorough
- Test case design (17 cases) is comprehensive

**Recommended Implementation Order**:
Phase 0 (binary fixes) -> Phase 0.5 (TC-020 bridge docs) -> Phase 1 (Review) -> Phase 2 (Braintrust) -> Phase 3 (Implementation)

---

## Convergence Points

### Where Both Agree

| Topic | Conclusion | Confidence |
|-------|------------|------------|
| TC-013 is not aligned with implementations | Structural drift across field names, stdin schemas, launch interface, and binary integration | HIGH |
| Three binary bugs block TC-013 | `runInterWaveScript` stub, budget double-reservation, `background_pid` not written | HIGH |
| 4 helper binaries is over-engineering | Reduce to 1 or 0; Review workflow needs no helper binary | HIGH |
| TC-013 is actually 3 tickets | Review, Braintrust, Implementation have different complexity profiles | HIGH |
| Effort estimate is too low | 3-5 days is insufficient; 5-8 days is realistic (after prerequisite fixes) | HIGH |
| Stdin inline examples are structurally wrong | TC-009 schemas use entirely different required fields and nesting depth | HIGH |
| Mozart Phase 2.5 has its own drift | TC-018's stdin templates don't match TC-009 schemas -- a second layer of misalignment | MEDIUM |
| Two execution paths must coexist | MCP spawn (foreground) + gogent-team-run (background) per TC-019 | HIGH |

### Complementary Insights

| Einstein Contribution | Staff-Architect Contribution | Combined Value |
|----------------------|------------------------------|----------------|
| Category analysis: TC-013 conflates transition design, workflow rewrites, and binary specs | Line-by-line identification of every stale field name and schema path | Together they show both the *why* (structural information debt) and the *what* (specific corrections needed) |
| The "orchestrator" abstraction dissolves in background path -- what remains is config generation | Practical observation that Review needs no orchestrator LLM at all -- router generates config directly | Config generation is the actual task, not "rewriting orchestrator prompts" -- the ticket title itself is misleading |
| Novel decomposition: TC-013a/b/c ordered by complexity | Concrete implementation order: Review first (simplest, no interview), then Braintrust, then Implementation | Convergent recommendation with different justifications (theoretical complexity ordering vs. practical risk reduction) |
| Mozart Phase 2.5 stdin templates are a second drift layer | Detailed field-by-field comparison of TC-013 stale names vs. TC-009 actual names | Two independent drift sources means corrections must reconcile three things: TC-013 inline examples, Mozart Phase 2.5 templates, AND TC-009 schemas |

---

## Divergence Resolution

### Resolved Divergence 1: How Many Helper Binaries?

**Einstein**: Replace 4 binaries with 1 `gogent-team-init` with subcommands, or zero for Review/Braintrust (LLM generates config directly with a validation pass).

**Staff-Architect**: Reduce from 4 to 1, consolidating `gogent-team-init-review`, `gogent-team-init-impl`, `gogent-parse-specs`, and `gogent-compute-waves` into a single binary.

**Resolution**: Zero new binaries for Review and Braintrust. One new binary (`gogent-team-init`) for Implementation only.

**Reasoning**: For Review, the router already knows the file list and reviewer set -- generating 4-5 JSON files is trivial LLM work or even scriptable in bash. For Braintrust, Mozart already generates config + stdin files in Phase 2.5 (TC-018). Only Implementation requires non-trivial computation (DAG parsing, wave computation via Kahn's algorithm, dynamic member count) that justifies a Go binary.

Einstein's "zero binary" insight is correct for the simpler workflows. Staff-Architect's "consolidate to 1" is correct for Implementation. The combined answer is: 0 for Review/Braintrust, 1 for Implementation.

**Confidence**: High

---

### Resolved Divergence 2: Rewrite TC-013 vs. Patch TC-013

**Einstein**: Rewrite from ground truth. The ticket's framing ("rewrite orchestrator prompts") is itself wrong -- what's needed is config generation logic, not prompt rewrites. The ticket title is misleading.

**Staff-Architect**: Patch TC-013 using the pre-implementation notes (already partially done on 2026-02-07) and TC-020 bridge docs.

**Resolution**: Rewrite, not patch. Einstein is correct that the drift is too deep for incremental patching.

**Reasoning**: The pre-implementation notes added on 2026-02-07 are already 153 lines of corrections before the actual ticket content begins. When the corrections are longer than the corrected content, the document has negative net signal. Furthermore, TC-013's conceptual model ("rewrite orchestrator prompts") is wrong -- the actual task is "implement config generation and team launch for each workflow." The ticket should be decomposed into three workflow-specific tickets with fresh specifications written against the actual Go source and TC-009 schemas.

The TC-020 bridge docs are the right intermediate step -- they document the corrections needed. But the bridge docs should feed into new tickets, not patch the existing TC-013.

**Confidence**: High

---

### Resolved Divergence 3: Fix Bugs Before or During TC-013?

**Einstein**: Fix binary bugs first. Budget double-reservation is a blocker; runInterWaveScript stub makes braintrust produce garbage; background_pid omission makes launch verification impossible.

**Staff-Architect**: Create a prerequisite ticket for binary fixes (Phase 0, 0.5-1 day).

**Resolution**: Fix bugs first, as a separate prerequisite. Both agree but Staff-Architect provides the cleaner operational framing.

**Reasoning**: These bugs exist in the Go binary (TC-008's deliverable), not in TC-013's domain. Fixing them during TC-013 would conflate two concerns and make the orchestrator rewrite harder to validate. A 0.5-1 day prerequisite ticket with three focused fixes is the cleanest path.

**Confidence**: High

---

### Unresolved Tensions

| Topic | Einstein View | Staff-Architect View | Why Unresolved |
|-------|--------------|---------------------|----------------|
| Mozart Phase 2.5 stdin template drift | Identified as a second drift layer requiring reconciliation | Not explicitly addressed (focused on TC-013 inline examples, not mozart.md) | Needs someone to actually compare mozart.md Phase 2.5 templates against TC-009 schemas and produce a correction list |

**Recommendation for Unresolved**: When implementing the Braintrust workflow (TC-013b), the first step must be reconciling mozart.md Phase 2.5 templates with TC-009 schemas. This is part of the Braintrust ticket's scope, not a separate prerequisite.

---

## Unified Recommendations

### Primary Recommendation

**Decompose TC-013 into four work items and execute in order.**

| Work Item | Description | Effort | Prerequisites |
|-----------|-------------|--------|---------------|
| **TC-013-prereq: Binary Bug Fixes** | Fix 3 bugs in `gogent-team-run`: (1) wire `runInterWaveScript` to actually execute the script, (2) remove double budget reservation in spawn.go (wave.go already reserves), (3) write `BackgroundPID` to config.json in main.go after startup | 0.5-1 day | None |
| **TC-020: Bridge Docs** (in progress) | Complete the ReviewOrch and ImplMgr bridge docs + decision matrix. These serve as the specification source for the three workflow tickets. | 1-2 days | None |
| **TC-013a: Review Workflow** | Router generates config.json + 4 reviewer stdin files + launches `gogent-team-run`. No LLM orchestrator needed. No helper binary needed. Simplest workflow -- validates the pattern. | 1-2 days | TC-013-prereq, TC-020 |
| **TC-013b: Braintrust Workflow** | Mozart interview (foreground) then generates config.json + stdin files + launches `gogent-team-run`. Requires reconciling mozart.md Phase 2.5 templates with TC-009 schemas. Inter-wave script integration. | 2-3 days | TC-013a (validates pattern), TC-013-prereq |
| **TC-013c: Implementation Workflow** | Router parses specs.md, computes wave DAG, generates dynamic config.json + stdin files + launches `gogent-team-run`. Requires `gogent-team-init` binary for DAG computation. Most complex. | 2-3 days | TC-013b (validates multi-wave pattern) |

**Total revised effort**: 5-8 days (implementation) + 1-2.5 days (prerequisites) = 6.5-10.5 days

**Rationale**: This recommendation integrates Einstein's complexity-ordered decomposition with Staff-Architect's practical phasing. Starting with Review validates the config generation + launch + verification pattern before tackling the more complex workflows. Each subsequent ticket builds on proven patterns from the previous one.

**Theoretical Support**: Einstein's category analysis shows that the three workflows have fundamentally different concerns (no LLM orchestrator for Review, interview-then-background for Braintrust, DAG computation for Implementation). Treating them as one ticket forces the implementer to context-switch between these patterns.

**Practical Validation**: Staff-Architect's implementation ordering (Review -> Braintrust -> Implementation) is confirmed by complexity analysis. Staff-Architect's bug inventory proves that prerequisites are real blockers, not theoretical concerns.

### Secondary Recommendations

1. **Retire TC-013 as a meta-ticket**: Keep it as the parent/tracking ticket that references TC-013a/b/c. Its inline implementation details are too stale to be useful -- the bridge docs and new tickets supersede them.
   - Priority: HIGH
   - Supports: Eliminating confusion from stale specifications

2. **Reconcile mozart.md Phase 2.5 with TC-009 schemas**: As part of TC-013b, the first task is comparing Mozart's stdin templates against the actual TC-009 schema definitions and producing corrected templates.
   - Priority: HIGH
   - Supports: Preventing drift-on-drift from propagating into implementation

3. **Add `background_pid` write to main.go startup**: The fix is a 3-line change in main.go after `NewTeamRunner()` -- set `runner.config.BackgroundPID` to `os.Getpid()` and call `runner.SaveConfig()`. This is part of TC-013-prereq.
   - Priority: CRITICAL
   - Supports: Launch verification (TC-013's step 7 in all three workflows)

4. **Wire `runInterWaveScript` to execute the binary**: Replace the stub (wave.go:96-102) with actual `exec.CommandContext` invocation. The binary (`gogent-team-prepare-synthesis`) already exists and takes a team directory as its sole argument. This is part of TC-013-prereq.
   - Priority: CRITICAL
   - Supports: Braintrust workflow (Beethoven receives synthesis inputs)

5. **Remove budget reservation from spawn.go**: wave.go:34 already calls `tryReserveBudget`. spawn.go:63-66 calls it again inside `Spawn()`. Remove the spawn.go reservation and its reconciliation-on-failure path (lines 66-76). The wave-level reservation is the correct location because it gates the goroutine launch. This is part of TC-013-prereq.
   - Priority: CRITICAL
   - Supports: Any multi-agent workflow actually starting

### Not Recommended

1. **Patching TC-013 in place**: The inline examples are too far from reality. The pre-implementation notes (153 lines of corrections) demonstrate that the correction overhead exceeds the document's value.

2. **Building all 4 helper binaries as specified**: `gogent-team-init-review` and `gogent-team-init-impl` (as separate binaries) add build/install/maintenance cost without proportional value. Review config generation is trivial; Braintrust config generation is already in Mozart.

3. **Implementing all three workflows simultaneously**: Sequential implementation with validation at each step catches integration issues early and prevents wasted effort on workflows that depend on patterns not yet proven.

---

## Implementation Pathway

### Phase 0: Binary Bug Fixes (0.5-1 day)

- [ ] **Fix 1**: Wire `runInterWaveScript` in wave.go to execute the script via `exec.CommandContext`. The script command is `gogent-team-prepare-synthesis <team-dir>`. Expand `$TEAM_DIR` variable in the script string. Test: verify `pre-synthesis.md` is written after Wave 1 completion.
- [ ] **Fix 2**: Remove budget reservation from `claudeSpawner.Spawn()` (spawn.go:63-76). Budget reservation already happens in `runWaves()` at wave.go:34-35. Test: verify a $6 budget can run 2 Opus agents ($5 estimated each) because wave-level reservation + reconciliation handles the lifecycle correctly.
- [ ] **Fix 3**: Write `BackgroundPID` to config.json in main.go after `NewTeamRunner()`. Add `pid := os.Getpid(); runner.config.BackgroundPID = &pid; runner.SaveConfig()` after line 73 of main.go. Test: after launch, `jq '.background_pid' config.json` returns a valid PID.
- [ ] Run `go test -race ./cmd/gogent-team-run/...` to verify no regressions.

### Phase 0.5: Complete TC-020 Bridge Docs (1-2 days)

- [ ] Complete `ReviewOrch-team-bridge.md` with corrected stdin examples validated against `schemas/stdin/reviewer.json`
- [ ] Complete `ImplMgr-team-bridge.md` with corrected config.json showing nested `waves[].members[]` structure
- [ ] Complete `team-dispatch-decision-matrix.md` with per-workflow path recommendations
- [ ] Update TC-013 to reference bridge docs and mark its inline examples as superseded

### Phase 1: Review Workflow -- TC-013a (1-2 days)

- [ ] Implement `/review` skill's team dispatch path
- [ ] Router computes git diff, classifies files, selects reviewers
- [ ] Router generates config.json from review template (no helper binary -- direct JSON generation)
- [ ] Router generates stdin files per reviewer (validated against `schemas/stdin/reviewer.json`)
- [ ] Router launches `gogent-team-run <team-dir>`
- [ ] Router verifies `background_pid` in config.json
- [ ] Router returns immediately with team status message
- [ ] Test: 3 successful review runs with different file types
- [ ] Add fallback to MCP spawn path for single-reviewer cases (per TC-019 decision matrix)

### Phase 2: Braintrust Workflow -- TC-013b (2-3 days)

- [ ] Reconcile mozart.md Phase 2.5 stdin templates with TC-009 schemas
- [ ] Update Mozart's Phase 6 to include `gogent-team-run` dispatch path alongside MCP spawn
- [ ] Verify inter-wave script execution (depends on Phase 0 Fix 1)
- [ ] Verify Beethoven receives `pre-synthesis.md` with Einstein and Staff-Architect outputs
- [ ] Test: 3 successful braintrust runs producing analysis documents at `.claude/braintrust/analysis-*.md`
- [ ] Add fallback to MCP spawn path for single-agent cases (Einstein-only braintrust)

### Phase 3: Implementation Workflow -- TC-013c (2-3 days)

- [ ] Build `gogent-team-init` binary (consolidation of `gogent-parse-specs` + `gogent-compute-waves`)
- [ ] Implement specs.md parsing and DAG computation (Kahn's algorithm)
- [ ] Router invokes `gogent-team-init` to generate config.json + stdin files
- [ ] Router launches `gogent-team-run <team-dir>`
- [ ] Test: 3 successful implementation runs with different DAG structures (1 wave, 2 waves, 3+ waves)
- [ ] Test: circular dependency detection
- [ ] Add fallback to direct Task() delegation for single-task tickets (no specs.md)

### Decision Points

1. **After Phase 1 completion**: Does the review workflow's config generation pattern validate cleanly? If yes, proceed to Phase 2. If significant issues surface, fix before attempting the more complex Braintrust workflow.

2. **During Phase 2**: Does Mozart's config generation work with the corrected stdin schemas? If Mozart's Phase 2.5 templates require major surgery, allocate additional time.

3. **Before Phase 3**: Is `gogent-team-init` justified, or can Implementation config generation be done by the LLM (like Review)? The answer depends on how complex the specs.md parsing turns out to be in practice.

---

## Risk Assessment

### Identified Risks

| Risk | Source | Likelihood | Impact | Mitigation |
|------|--------|------------|--------|------------|
| Budget double-reservation blocks all multi-agent launches | Both | CERTAIN (code confirmed) | CRITICAL | Phase 0 Fix 2 |
| `runInterWaveScript` stub produces empty Beethoven inputs | Both | CERTAIN (code confirmed) | CRITICAL | Phase 0 Fix 1 |
| `background_pid` null causes false launch failure reports | Both | CERTAIN (code confirmed) | HIGH | Phase 0 Fix 3 |
| Mozart Phase 2.5 templates have their own drift vs TC-009 | Einstein | HIGH | HIGH | Reconcile during TC-013b |
| TC-013 inline examples mislead implementer | Both | HIGH (if TC-013 not retired) | MEDIUM | Retire TC-013 as meta-ticket, use bridge docs |
| specs.md format is not canonically defined | Einstein | MEDIUM | MEDIUM | Define format as part of TC-013c |
| Two execution paths (MCP + team-run) create confusion | Einstein | MEDIUM | MEDIUM | Decision matrix (TC-020 Deliverable 3) |

### Assumptions to Validate

| Assumption | How to Validate | Before Phase |
|------------|-----------------|--------------|
| Review config generation is simple enough for LLM/router | Implement it in Phase 1 and measure complexity | Phase 1 |
| Mozart Phase 2.5 templates can be reconciled with TC-009 | Side-by-side comparison of mozart.md templates vs TC-009 required fields | Phase 2 |
| `gogent-team-prepare-synthesis` produces valid pre-synthesis.md when given actual agent outputs | Run binary with sample stdout files from a real braintrust session | Phase 0 |
| specs.md format is consistent enough for automated parsing | Survey existing specs.md files in the codebase | Phase 3 |

---

## Open Questions

1. **Who owns the `runInterWaveScript` stub fix?** TC-010 was marked complete, but its binary is never called. This is either a TC-008 bug (wave.go should have been updated) or a TC-010 oversight (integration wiring was out of scope). Regardless, it needs to be fixed before TC-013b can work. Recommendation: Assign to TC-013-prereq.

2. **Should budget double-reservation be fixed before or during TC-013?** Before. The bug exists in the Go binary's core spawn path and affects ALL team workflows, not just the orchestrator rewrites. It is a TC-008 defect. Recommendation: TC-013-prereq, tested immediately.

3. **What is the canonical specs.md format?** TC-013 shows a markdown format with `## Tasks` sections containing `**Task ID**`, `**Title**`, `**Agent**`, `**Files**`, `**Blocked By**` fields. But no formal spec exists. The `gogent-team-init` binary needs a defined input format. Recommendation: Define as part of TC-013c, or accept that the LLM can parse loosely-structured markdown.

4. **Should TC-013 be retired or rewritten?** Einstein argues rewrite; this analysis recommends retirement as a meta-ticket. The bridge docs + three new workflow tickets provide clearer, ground-truth-aligned specifications. Keeping TC-013's 1340 lines of mostly-stale implementation detail creates confusion.

5. **Does the feature flag (`use_team_pattern`) need implementation?** TC-013 mentions `settings.json: "use_team_pattern": true/false` for fallback. This is architecturally correct but adds implementation cost. Recommendation: Implement as part of TC-013a (Review) since it is the first workflow to ship and needs the fallback mechanism proven.

---

## Appendix: Verified Bug Details

### Bug C-1: `runInterWaveScript` Stub

**File**: `/home/doktersmol/Documents/GOgent-Fortress/cmd/gogent-team-run/wave.go`, lines 96-102

```go
func runInterWaveScript(ctx context.Context, scriptPath string, teamDir string) error {
    log.Printf("[INFO] wave: inter-wave script %s (TC-010 stub, not yet implemented)", scriptPath)
    // TC-010 will implement real execution here
    return nil
}
```

**Impact**: In the braintrust workflow, `waves[0].on_complete_script` is set to `"gogent-team-prepare-synthesis $TEAM_DIR"`. This function is called (wave.go:51-55) but does nothing. The `pre-synthesis.md` file is never created. Beethoven (Wave 2) receives no inter-wave synthesis input and must work from raw stdout files alone -- or worse, references a non-existent file and errors.

**Fix**: Replace with `exec.CommandContext` execution, expanding `$TEAM_DIR` to the actual team directory path.

---

### Bug C-2: Budget Double-Reservation

**File 1**: `/home/doktersmol/Documents/GOgent-Fortress/cmd/gogent-team-run/wave.go`, lines 34-35
```go
estimated := tr.estimateCost(wave.Members[memberIdx].Agent)
if !tr.tryReserveBudget(estimated) {
```

**File 2**: `/home/doktersmol/Documents/GOgent-Fortress/cmd/gogent-team-run/spawn.go`, lines 63-66
```go
estimatedCost := tr.estimateCost(cfg.agentID)
if !tr.tryReserveBudget(estimatedCost) {
    return fmt.Errorf("insufficient budget: need $%.4f, have $%.4f", estimatedCost, tr.BudgetRemaining())
}
```

**Call chain**: `runWaves` -> `tryReserveBudget` ($5) -> goroutine -> `spawnAndWaitWithBudget` -> `spawnAndWait` -> `tr.spawner.Spawn()` -> `claudeSpawner.Spawn()` -> `tryReserveBudget` ($5 again).

**Impact**: For a braintrust workflow with $6 budget and 2 Opus agents: Wave reserves $5 for Einstein (remaining: $1). Wave reserves $5 for Staff-Architect (remaining: -$4, fails). Even if wave succeeds, Spawn() tries to reserve $5 AGAIN inside the goroutine. The reconciliation path in `spawnAndWaitWithBudget` (wave.go:88) reconciles the wave-level reservation but cannot reconcile the spawn-level reservation because it was never tracked at that level. Net result: budget is depleted before any agent starts.

**Fix**: Remove `tryReserveBudget` call from `claudeSpawner.Spawn()` (spawn.go:63-76). The wave-level reservation is the correct budget gate.

---

### Bug C-3: `background_pid` Never Written to Config

**File**: `/home/doktersmol/Documents/GOgent-Fortress/cmd/gogent-team-run/main.go`

The binary writes a PID *file* at `daemon.go:62` (`acquirePIDFile`), but never sets `config.BackgroundPID` in the TeamConfig struct. Searching main.go for `BackgroundPID` yields zero results. The field exists in the struct definition (`config.go:30`) but is never populated.

**Impact**: TC-013's launch verification (lines 386-392 in all three workflows) reads `jq -r '.background_pid' "$team_dir/config.json"` and treats `null` as launch failure. Since the field is never set, EVERY launch reports failure despite the daemon running correctly.

**Fix**: Add to main.go after `NewTeamRunner()` (after line 73):
```go
pid := os.Getpid()
runner.config.BackgroundPID = &pid
if err := runner.SaveConfig(); err != nil {
    log.Fatalf("Failed to write background PID to config: %v", err)
}
```

---

## Metadata

```yaml
braintrust_analysis_id: tc013-alignment-review-2026-02-08
problem_brief_id: tc013-alignment
timestamp: 2026-02-08T12:00:00Z

agents_invoked:
  - mozart
  - einstein
  - staff-architect-critical-review
  - beethoven

convergence_points: 8
divergence_points: 3
contradictions_resolved: 3
contradictions_unresolved: 0

recommendations_count: 8 (1 primary decomposition + 5 secondary + 2 not-recommended)
risks_identified: 7
open_questions: 5

source_files_examined:
  - tickets/team-coordination/tickets/TC-013.md (1340 lines)
  - tickets/team-coordination/tickets/TC-020.md (636 lines)
  - cmd/gogent-team-run/wave.go (103 lines)
  - cmd/gogent-team-run/spawn.go (449 lines)
  - cmd/gogent-team-run/config.go (385 lines)
  - cmd/gogent-team-run/daemon.go (276 lines)
  - cmd/gogent-team-run/main.go (137 lines)
  - .claude/schemas/stdin/einstein.json (177 lines)
  - .claude/braintrust/synthesis-team-coordination.md (788 lines)
  - .claude/braintrust/einstein-team-coordination-analysis.md (293 lines)
  - .claude/braintrust/staff-architect-team-coordination-review.md (478 lines)

critical_bugs_verified: 3 (all confirmed against source code)
```
